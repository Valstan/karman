# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-08-10
**Branch:** main
**Прод:** Бокс 1 (`831d0ce99bdf.vps.myjino.ru`, PG16, app :3002). Задеплоен `ab1bb95`,
три деплоя сессии зелёные, смоук `health=200` / `root=307`, юнит `active`.
Миграций не было. Прод-конфиг тронут один раз: systemd-юнит пересобран из репо
(`KEEP_ALIVE_TIMEOUT=65000`), бэкап прежнего — `/etc/systemd/system/karman.service.bak-20260810`.
В vault 12 комнат; комнате `gonba` выдан первый rw-токен.

---

## Текущая нитка

**Волна 2 ADR-0012 — паспортный сервер.** GO пришло письмом brain 08-07: гейт волны 1 закрыт
с запасом (читателей vault четыре — trener, GONBA, вМалмыже, setka). Кода пока нет ни строки,
форма верификатора выбрана в прошлой сессии: **удалённый JWKS + cooldown + кеш stale-if-error**
(probe с бокса зелёный, Сетка подтвердила со своей стороны). Пилот — trener, принял 08-01, ждёт.

## Следующий шаг

Начать волну 2 **со спеки и миграции, а не с кода** — миграция блокирует авто-деплой и
применяется руками, поэтому она должна быть первой, а не догонять готовый роут.

1. Перечитать спеку: `../brain_matrica/adr/0012-ecosystem-passport.md` §5 (форма роута) и §6
   (долги). Из §6 закрыт `VAULT_PROVISION_KEY` в `.env.example` (`ab1bb95`); остаётся
   **колонка актора в аудите** — её и тащить той же миграцией.
2. Миграция `lib/db/migrations/*.sql`: кеш-таблица JWKS (ключи + `fetched_at` для
   stale-if-error) + колонка актора в `secrets_audit`. Применять по граблю из шапки
   «Прод-инфра» ниже, затем `gh workflow run deploy-prod.yml --ref main`.
3. Роут `POST /api/secrets/session` (`app/api/secrets/session/route.ts`), рядом с
   `app/api/secrets/route.ts`; личность — по неизменяемому числовому id, rate-limit по
   **доказанному** claim'у (сегодняшний бакет по `x-forwarded-for` в `lib/secrets/rate-limit.ts`
   — декорация), TTL-сессии, `GET /api/secrets/self`, самоотзыв.
4. **Машинный grant в v1 не проектировать** — вырезан адверсариальной проверкой (RCE на чужом
   проде). Это решение, а не забывчивость.
5. Отчёт brain по факту первой вехи (`ref:` — `2026-08-07-wave-2-go-passport-server`).

## Контекст

- **Связанные коммиты сессии:** `89e5278` (#88 — канон: чужие репо read-only, mailbox в два
  канала), `38870be` (#89 — G234: `KEEP_ALIVE_TIMEOUT` + сверка юнита с продом), `c417e20`
  (#90 — токен комнаты `gonba` выдан и доставлен), `429b0d6` (#91 — deadcode, август),
  `ab1bb95` (#92 — `.env.example`).
- **План:** ADR-0012 §5 (у brain). Опись гейтов — `docs/gates.md`, канон агентов — `AGENTS.md`.
- **Открытые PR:** нет — все пять смержены.
- **Открытые вопросы для пользователя:**
  - **бэкап `media/`** — единственный риск потери данных в списке отложенного, третий перенос
    подряд, решения так и нет;
  - **квартальный самоосмотр Q3** — окно авг–сен открыто, нужен go на отдельную сессию;
  - разрешение на запись в секреты соседнего репозитория дано разово (Gonba) — brain'у
    предложено оформить это именованным исключением в ADR-0007, ответа пока нет.

## Failed approaches (этой нитки)

- **Выдать токен `gonba` штатным `POST /api/secrets/provision` — не вышло, 409.** Гейт
  `provisionFirstToken` требует «0 секретов, 0 карточек, токен не использован», а в комнату
  сегодня в 09:41 UTC положили `DEEPSEEK_API_KEY` через GUI. Обходить ослаблением гейта
  **отвергнуто**: проверка на пустоту защищает содержимое заранее наполненных комнат от
  держателя `VAULT_PROVISION_KEY`. Выдали транзакцией на проде с разрешением владельца (#025).
- **Пара `keepAliveTimeout` + `headersTimeout` (совет brain) — вторая переменная не нужна.**
  Проба на боевом Node 22.20.0: idle-сокет пережил 62 с при `headers=60000`. С Node 18 таймер
  заголовков стартует с первым байтом запроса, а не с простоя. У Next-standalone второй ручки
  и нет — только `KEEP_ALIVE_TIMEOUT`.

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md`. Витрина: **бэкап `media/`** (риск потери
данных, snoozed 3); **переоткрытие комнаты владельцем** — новый тех-долг, гейт ADR-0010 считает
признаком живой комнаты её наполненность и запирает дверь перед тем, кто готовил комнату
к заселению; **migration-guard защищает один push, а не инвариант** (актуально прямо сейчас —
следующий шаг несёт миграцию); квартальный самоосмотр Q3; deadcode — следующий прогон в сентябре.

## Прод-инфра (для следующей сессии)

- **ssh на Бокс 1 есть:** алиасы `karman` и `GONBA` (ключ `id_ed25519_gonba_deploy`).
  Деструктив на прод-БД — только с подтверждением владельца в том же ходе (#025).
- **Грабля psql по ssh:** `$DATABASE_URL` в env юзера НЕТ — переменные в
  `/etc/karman/karman.env`. Рабочая форма:
  `ssh karman 'set -a; . /etc/karman/karman.env; psql "$DATABASE_URL" -c "…"'`.
  Строки с `$` в двойных кавычках раскрывает удалённый шелл — литералы слать как `$$…$$`
  или SQL целиком через stdin (`psql -v ON_ERROR_STOP=1 -f -` — так шла выдача токена `gonba`).
  Колонки аудита: `at`, `action`, `detail` (не `created_at`/`details`).
- **Секрет через ssh/gh — не через argv.** Токен `gonba` доставлен так: значение только
  в памяти процесса, в БД — sha256-хэш, в проверку — через `curl --config -` (stdin, не argv,
  иначе видно в `ps`), в GitHub — через stdin `gh secret set` (шифрует пубключом репо
  до отправки). Ни файла, ни лога, ни истории шелла.
- **systemd-юнит деплой НЕ синхронизирует** — только `systemctl restart`. Правка юнита
  применяется руками: `git show HEAD:scripts/karman.service | ssh karman 'sudo tee …'` +
  `daemon-reload` + `restart`. Файл в репо сверен с боевым 08-10 (до этого расходился молча).
- **Внешние URL проверять С БОКСА, а не с PC40:** с рабочей машины исходящий HTTPS идёт
  через прокси и таймаутит. `gh` при этом с PC40 работает нормально.
- **`main` защищён технически:** required check `gates`, PR обязателен, `enforce_admins: true`,
  force-push и удаление ветки запрещены. Мержить после зелёного CI:
  `gh pr merge <N> --squash --delete-branch` (без `--admin`).
- **Миграции:** `deploy-prod.yml` блокирует авто-деплой на коммите с новой миграцией →
  применить psql'ом с машины, затем `gh workflow run deploy-prod.yml --ref main`. Guard
  видит только последний коммит (тех-долг выше).
- **Vault:** `POST /api/secrets/provision` под `VAULT_PROVISION_KEY` — заводит комнату И
  выдаёт первый токен **нетронутой** комнате (ADR-0010; наполненная через GUI комната уже
  не проходит — см. Failed approaches). Grant между комнатами — GUI владельца под 2FA,
  машинного grant не будет. Бэкап vault: cron 03:30, `scripts/backup_vault.sh`.
