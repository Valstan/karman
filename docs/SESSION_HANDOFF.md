# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-08-10
**Branch:** main
**Прод:** Бокс 1 (`831d0ce99bdf.vps.myjino.ru`, PG16, app :3002). Задеплоен `18c9326`,
деплой через `workflow_dispatch` (auto-деплой заблокировал migration-guard — так и задумано),
смоук `health=200`, новые роуты отвечают `401` без удостоверения. **Миграция `0007`
применена** на живой БД с подтверждением владельца (#025): 4 таблицы `passport_*`,
`secrets_token += expires_at/identity_id`, `secrets_audit += actor`, строка issuer'а GitHub.
В vault 12 комнат; реестр личностей паспорта пуст (0 строк) — это норма, доступ даёт только
явная строка.

---

## Текущая нитка

**Волна 2 ADR-0012 — паспортный сервер. Веха 1 закрыта** (`18c9326`, PR #94): проект
предъявляет подписанное удостоверение своего CI и получает короткоживущий токен своей
комнаты. Общий `VAULT_PROVISION_KEY` стал break-glass'ом за флагом `PROVISION_KEY_ENABLED`.

Форма, выбранная в прошлой сессии, реализована: удалённый JWKS + cooldown + снимок в БД
для stale-if-error. Контракт, runbook владельца и рецепт клиента — `docs/passport-server.md`.

## Следующий шаг

**Гейт волны 2 («зелёный смоук без кликов владельца») НЕ закрыт, и закрыть его в одиночку
нельзя** — удостоверение минтит CI *чужого* репозитория (пилот — trener), а писать в чужие
репо запрещено. Отчёт brain с этим требованием отправлен 08-10.

1. **Ждём пилота.** Владельцу — завести ОДНУ строку `passport_identity` по runbook'у
   (`gh api repos/Valstan/trener --jq .id` + id комнаты из живого `/secrets`); trener —
   добавить `permissions: id-token: write` и шаг запроса удостоверения. Признак закрытия
   гейта: в аудите комнаты появляется `session_open`.
2. **Своя веха 2 (не блокирует пилота): GUI-раздел реестра личностей** в `/secrets` —
   завести/отозвать личность кнопкой, а не psql'ом. Отзыв в UI обязан гасить и живые сессии
   (каскад), хотя проверка на чтении уже страхует.
3. Гейт «таблицы схемы vs список дампа в `backup_vault.sh`» — см. отложенное; класс, а не
   разовый промах.

## Контекст

- **Связанные коммиты сессии:** `18c9326` (#94 — паспортный сервер, веха 1).
- **План:** ADR-0012 §5 (у brain), реализация и границы — `docs/passport-server.md`.
- **Открытые PR:** нет.
- **Открытые вопросы для пользователя:**
  - **бэкап `media/`** — единственный риск потери данных в списке отложенного, третий перенос
    подряд, решения так и нет;
  - **квартальный самоосмотр Q3** — окно авг–сен открыто, нужен go на отдельную сессию;
  - разрешение на запись в секреты соседнего репозитория дано разово (Gonba) — brain'у
    предложено оформить это именованным исключением в ADR-0007, ответа пока нет.

## Failed approaches (этой нитки)

- **`createRemoteJWKSet` из jose как кеш ключей — не подошёл.** Его кеш живёт в памяти
  процесса (не переживает рестарт) и падает вместе с недоступным issuer'ом. Форма
  stale-if-error требует снимка в БД — сделан `passport_jwks_cache`.
- **Сессия как новый вид предъявителя — отвергнута в пользу «обычный токен комнаты со
  сроком».** Так клиент волны 1 меняется в одной строке, весь контур `/api/secrets`
  (grant'ы, аудит, allowlist) работает без правок, а каскад отзыва достаётся даром.
- **Выключить `PROVISION_KEY_ENABLED` по умолчанию (как в §7 ADR) — не стали.** Паспорт
  ещё не прошёл пилот; гашение живого канала до пилота — поломка под видом ужесточения.
  Волна 5 теперь стоит одной переменной без выкатки кода.
- **`revokeIdentity`/`countLiveSessions` в сервисе — написаны и удалены до коммита.**
  Вызывающих у них не было бы до GUI-вехи, а это ровно тот born-unused класс, что
  рецидивировал трижды. Отзыв личности пока — SQL-runbook + проверка на каждом чтении.

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md`. Витрина: **бэкап `media/`** (риск потери
данных, snoozed 3); **дамп бэкапа не связан со схемой** (новое — `secrets_grant` месяц не
бэкапился); **переоткрытие комнаты владельцем**; **migration-guard защищает один push, а не
инвариант** (сегодня сработал правильно, но по-прежнему смотрит только последний коммит);
квартальный самоосмотр Q3; deadcode — следующий прогон в сентябре.

## Прод-инфра (для следующей сессии)

- **ssh на Бокс 1 есть:** алиасы `karman` и `GONBA` (ключ `id_ed25519_gonba_deploy`).
  Деструктив на прод-БД — только с подтверждением владельца в том же ходе (#025).
- **Грабля psql по ssh:** `$DATABASE_URL` в env юзера НЕТ — переменные в
  `/etc/karman/karman.env`. Рабочая форма:
  `ssh karman 'set -a; . /etc/karman/karman.env; psql "$DATABASE_URL" -c "…"'`.
  Строки с `$` в двойных кавычках раскрывает удалённый шелл — литералы слать как `$$…$$`
  или SQL целиком через stdin (`psql -v ON_ERROR_STOP=1 -f -` — так шла миграция `0007`:
  `git show HEAD:lib/db/migrations/0007_*.sql | ssh karman '… psql … -f -'`).
  Колонки аудита: `at`, `action`, `detail`, `actor` (не `created_at`/`details`).
- **Секрет через ssh/gh — не через argv** (`curl --config -`, `gh secret set` через stdin).
- **systemd-юнит деплой НЕ синхронизирует** — только `systemctl restart`. Правка юнита
  применяется руками: `git show HEAD:scripts/karman.service | ssh karman 'sudo tee …'` +
  `daemon-reload` + `restart`. Файл в репо сверен с боевым 08-10.
- **Внешние URL проверять С БОКСА, а не с PC40:** с рабочей машины исходящий HTTPS идёт
  через прокси и таймаутит. `gh` при этом с PC40 работает нормально.
- **`main` защищён технически:** required check `gates`, PR обязателен, `enforce_admins: true`.
  Мержить после зелёного CI: `gh pr merge <N> --squash --delete-branch` (без `--admin`).
- **Миграции:** `deploy-prod.yml` блокирует авто-деплой на коммите с новой миграцией →
  применить psql'ом с машины, затем `gh workflow run deploy-prod.yml --ref main`. Guard
  видит только последний коммит (тех-долг выше).
- **Vault:** штатный вход — паспорт CI (`POST /api/secrets/session`, `docs/passport-server.md`);
  `POST /api/secrets/provision` под `VAULT_PROVISION_KEY` — break-glass, гасится
  `PROVISION_KEY_ENABLED=false`. Grant между комнатами — GUI владельца под 2FA, машинного
  grant не будет. Бэкап vault: cron 03:30, `scripts/backup_vault.sh` (список таблиц
  догоняет схему вручную — см. отложенное).
