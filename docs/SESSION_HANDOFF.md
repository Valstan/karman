# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-06-28
**Branch:** main
**Прод:** **Бокс 1** (`831d0ce99bdf.vps.myjino.ru`, PostgreSQL 16.14, app :3002),
крутит `b72023d` (Zod-фикс включён). `karman` + `karman-reminders` = active, воркер —
единственный поллер Telegram. Старый tiny **удалён владельцем** (откат закрыт).

---

## Текущая нитка

**`/payments` достроен и в проде (#41, `48d107c`, 2026-06-26).** Owner-решение из письма brain
`2026-06-25-payments-build-it` (recommend): достроить спящую фичу `listPayments`, не удалять.
Сделано как UI-поверхность поверх существующего домена платежей (без миграций): серверная
страница `app/(app)/payments/page.tsx` (`requireUser` → `listPayments`), таблица
`components/app/all-payments-table.tsx` (mark-paid через P4-`updatePaymentAction` — идемпотентно/
обратимо #025, edit/delete, ссылка на кредит, статус-бейдж), nav-ссылка «Платежи» в хедере.
Через стандартный #027-flow (гейты → PR → CI → авто-мерж → `deploy-prod.yml`). Побочно: спящий
`listPayments` подключён → ушёл из `npm run deadcode`, пункт вычеркнут из `PENDING_FOLLOWUPS.md`.

**Доклад brain отправлен** — `mailbox/to-brain/2026-06-28-payments-page-delivered.md` (feedback,
`ref: 2026-06-25-payments-build-it`); brain этой доставки ждал.

**KARMAN между нитками.** Катовер tiny → Бокс 1 закрыт (Ф3–Ф8, 2026-06-16); остаётся хвост —
живой TG-тест кнопки (за владельцем). Новых директив brain нет.

## Следующий шаг

1. **Живой TG-тест кнопки (действие владельца)** — открыть бота, нажать кнопку напоминания /
   отправить команду → убедиться, что отвечает (ingest + write-back уже на Боксе 1). После этого —
   письмо brain с подтверждением → катовер закрыт на 100%.
2. **Будущие прод-миграции на Боксе 1 — через brain** (RACI playbook §1, brain 2026-06-16):
   готовлю SQL по migration-guard-паттерну → brain применяет psql'ом. **ssh-доступа к Боксу 1
   у меня нет**, локальный `ssh karman` указывает на УДАЛЁННЫЙ tiny — не использовать; живость
   прода проверять HTTP-пробой `https://831d0ce99bdf.vps.myjino.ru/api/health`. См. память
   `karman-prod-box1-no-ssh`.

## Контекст
- **План:** нет.
- **Связанные коммиты сессии:** `48d107c` (#41 страница `/payments`), `b7cb654` (#40 ack re-триаж + adoption #027).
- **Открытые PR:** нет.
- **Открытые вопросы для пользователя:** живой TG-тест кнопки (физическое действие владельца, см. Следующий шаг).

## Failed approaches (этой нитки)

- **ssh на Бокс 1 локальным ключом (`id_ed25519_karman_deploy`)** — `Permission denied (publickey)`.
  Доступ к боксу за brain (CI-деплой-ключ); ключ на общий бокс не добавляют сознательно
  (минимизация key-surface, per-app-изоляция — отдельный шаг по слову владельца). Прод-ops веду
  через brain. Локальный `~/.ssh/config` алиас `karman` ещё на мёртвый tiny — для проверки прода
  не использовать, только HTTP-проба.

## Не забыть (low-priority)

Канонический список с метками — `docs/PENDING_FOLLOWUPS.md`. Витрина: живой TG-тест кнопки на
Боксе 1 (владелец); удалить тестовое напоминание id 1 на Боксе 1 (через brain — у меня нет DB-доступа);
P3-хвосты напоминаний (праздники / календарь UI / произвольные даты); P5 напоминаний (богатый
контент); бэкап `media/` на Боксе 1; квартальный самоосмотр Q3 2026.
