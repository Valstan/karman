# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-07-11
**Branch:** main
**Прод:** **Бокс 1** (`831d0ce99bdf.vps.myjino.ru`, PostgreSQL 16, app :3002), HEAD main `5f157bb`.
Vault полностью в проде: миграции `0004`/`0005` применены, деплои прошли, ночной бэкап-cron 03:30 живой.

---

## Текущая нитка

_Нет — vault-директива brain 06-30 (ADR-0006) закрыта целиком._ За сессию реализованы все 4 фазы
полного хранилища секретов + попутные фиксы; всё в проде и проверено владельцем на живую.

## Следующий шаг

Нитки нет — работа по запросу владельца. Кандидаты (из `docs/PENDING_FOLLOWUPS.md`):
- **Наполнить личную ячейку владельца** (пароли почты/банков) — комната без токена, GUI под 2FA,
  вручную или CSV-импортом (`/secrets`). Уже под ночным offsite-бэкапом.
- Остаток secrets по запросу: ротация мастер-ключа, scoped/TTL-токены, CLI-клиент.
- Квартальный стратегический самоосмотр — первый Q3 2026 (авг–сен).

## Контекст
- **План:** `docs/secrets-vault-plan.md` (все Ф1–Ф4 отмечены реализованными).
- **Связанные коммиты сессии:** `ca40a3e` (#60 план+ADR-0007+ре-триаж+deadcode), `61acc5e` (#61 Ф1
  карточки), `9170e22` (#62 Ф2 2FA), `e7e2d60` (#63 фикс счётчика), `1185513` (#64 Ф3 CSV),
  `0e0e1f2` (#65 Ф4 бэкап), `5f157bb` (#66 .gitattributes).
- **Открытые PR:** нет (кроме этого handoff-PR).
- **Открытые вопросы для пользователя:** нет. У владельца на руках приватный gpg-ключ бэкапов
  (FPR `99ECCA03…2817F37A`) — хранить надёжно, без него бэкапы не восстановить.

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md`. Витрина: наполнить личную ячейку владельца;
раздача secrets-токенов проектам (owner/project-driven); удалить тестовое напоминание id 1 на
Боксе 1 (DELETE → подтверждение #025); P3/P5-хвосты напоминаний; квартальный самоосмотр Q3 2026.

## Прод-инфра (для следующей сессии)

- **Бэкап vault:** `scripts/backup_vault.sh` живёт на боксе в СТАБИЛЬНОМ `/home/valstan/karman/scripts/`
  (деплой кладёт релизы в `~/karman/releases/<sha>`+симлинк current, scripts/ в артефакт НЕ входит).
  Cron valstan 03:30, лог `~/backups/vault-backup.log`. Ручной прогон:
  `ssh karman 'set -a; . /etc/karman/karman.env; set +a; bash /home/valstan/karman/scripts/backup_vault.sh'`.
- **2FA:** владелец входит под `admin` (auth_user.id 1), 2FA включена. При правках гейта /secrets
  учитывать claim `mfa` в сессии.
