# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю (в т.ч. подробности 11–14.09: `karman-tg`, G331, D-078, D-090,
> документы семьи #143–#152) смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-09-22
**Branch:** `main`
**Прод:** код #157 (древо семьи), `next` 16.3.5, миграции `0014`–`0018` применены
(`0018_family_tree` — psql 21.09, деплой `workflow_dispatch`). В env сервиса `MEDIA_ROOT` и
`BRAIN_MAP_GITHUB_TOKEN`. В `media/` 116 сканов, в `admin` 65 документов (#121 — рукописная
схема древа от Виталины); в древе 23 человека, 35 связей (сид 21.09 + схема 22.09, SQL в
scratchpad — персональные данные, в git нет).

---

## Текущая нитка

**Древо семьи** (`/documents/family`, задача владельца 21.09) — в проде первой версией, #157.
22.09 по рукописной схеме Виталины дозаполнено: Медведевы получили имена, добавлены
Лапухины (Кинерь), Боганов и Елизавета Медведева (Рожки), прапрабабушка «Канави»
(старообрядка), тётя Инна; национальности и места; сама схема — документ #121. Ждёт смоука
владельца глазами: «Древо», «Текстом», «Печать», «родство считать от». Дыры данных: годы
рождения только у Валентина; фамилия Филиппа; имя прапрабабушки; занятие Ольги по схеме
(неразборчиво). Сессия 21.09 до этого разобрала почту 14–18.09:

| Что | PR |
|---|---|
| G347: `[ rc = 255 ] && echo` → `if` в двух шагах `deploy-prod.yml`; строка Мозгу по G355 (`grep -q` под pipefail — нет) | #154 |
| Легаси-имена cookie сняты — парк до 18.09 истёк; даты D-096/D-097 в PENDING | #155 |
| D-096 строки 02.10 отправлены досрочно; D-097 три строки тем же письмом; правила #166/#125/#152 и память Мозга в `AGENTS.md` | #156 |
| Древо семьи: миграция `0018`, `lib/family/*` (родство и раскладка, 23 теста), сервис/actions, страница и SVG-древо, печать, кнопка в «Документах» | #157 |
| 22.09: раскладка PENDING — закрытое в `docs/pending-archive/2026-09-22-closed.md`, живой файл 47 КБ; потолок 50 КБ гейтом `PENDING_FOLLOWUPS.md size` в `ci.yml` (D-096/D-097, досрочно к 16.10) | #158 |
| 22.09: древо по схеме Виталины — 6 новых людей, 9 связей, правки 8 карточек; схема как документ #121 (psql по ssh, данных в git нет) | — |

## Следующий шаг

0. **Древо — по итогам смоука владельца:** дозаполнить годы и пробелы (см. PENDING), при
   желании — фото на узле (документ «Фотография» → аватар, как в карточках людей) и
   выгрузка древа в PDF/DOCX через уже подключённый `docx`.
1. **К 16.10 (D-096 / D-097, в PENDING):** #015 — таблица всех путей записи и требуемых
   прав (Server Actions, `app/api`, воркер напоминаний, `karman-tg`, синк ЕСА); в отчёте
   Мозгу — строка о раскладке PENDING (сделана 22.09). **К 30.10:** #057 состязательный
   аудит по рецепту Матрицы.
2. **Дайджест 12.09:** #312 `npm audit` по расписанию; G340 — нет метрики на страницах после
   входа (`grep -rn "mc.yandex\|ym(" app components`).
3. **Смоук владельца** по #145 (карточки людей, «Собрать выборку», тёмная тема) — не
   подтверждён. Пометка «уточнить по скану»: дата постановки на учёт в приписном Даниила (id 95).
4. Далее — `docs/PENDING_FOLLOWUPS.md`: R1 (тесты инвариантов доступа, Q4), G321, гейт
   таблиц бэкапа; deadcode — октябрь.

## Контекст

- **Планы и контракты:** `docs/ecosystem-map.md`, `docs/OPERATIONS.md`,
  `docs/secrets-client-guide.md`, `docs/passport-server.md`, `docs/esa-login.md`,
  `docs/telegram-reminders.md`.
- **Аккаунты прода:** `admin` (id 1, superuser, привязан к ЕСА), `Valstan` (id 17, отключён),
  `Chaka` (id 29). Документы семьи — все в `admin`, различаются полем `holder`.
- **Комнаты секретов:** 11. Личности паспорта: 1 trener, 2 Gonba, 3 Sabantuy, 4 Kazanskaya,
  5 vMalmyzhe, 6 DKMalmyzh.
- **Открытые PR:** нет. **Открытые вопросы для владельца:** где единственная копия
  закрытого gpg-ключа и `SECRETS_MASTER_KEY` (висит с августа).
- **Почта brain:** все письма по 18.09 включительно обработаны; отправлены 21.09 два
  письма (`mailbox/to-brain/2026-09-21-*`). Ждём от Мозга номера/ack на них.
- **Память Мозга читается по индексу** (D-097): `python ../brain_matrica/.claude/scripts/brain_find.py "…"`
  → один файл `gotchas/G<N>.md` / `ideas/NNN-*.md`. `GOTCHAS.md` целиком не грузить.

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md`. Витрина: **16.10 / 30.10** по D-096;
**смоук владельца**; **#312 / G340**; **R1**; **G321**; **гейт таблиц бэкапа**; deadcode —
октябрь. Фото для карточек: документ вида «Фотография» у каждого человека (пока ни одного).

## Прод-инфра (для следующей сессии)

- **ssh:** алиасы `karman` и `GONBA`. Первый коннект может отвалиться — повторить.
  Деструктив по прод-БД — только с подтверждением владельца в том же ходе (#025).
- **Порт приложения — 3002**, в юните. Env-файл сервиса — см. `EnvironmentFile` юнита
  (`/etc/karman` root-only: правки только руками владельца, секреты агент не вписывает).
- **nginx на боксе — хозяин GONBA**; `default_server` держит vhost Гоньбы, наш vhost
  отвечает только по своему `server_name` (проверено 21.09).
- **psql по ssh (D-046):** SQL в файл → `ssh karman 'cat > /tmp/<файл>' < <файл>` →
  `ssh karman 'envf=$(systemctl show karman -p EnvironmentFiles --value | cut -d" " -f1); set -a; . "$envf"; psql "$DATABASE_URL" -f /tmp/<файл>'`.
  `\pset pager off` первой строкой. Временные файлы с персональными данными — удалять с бокса.
- **Загрузка документов пачкой:** генератор SQL + tar по slug → на бокс → psql в транзакции
  с `ingest_map` → `mv` в `$MEDIA_ROOT/documents/1/<id>/` → сверка путей `document_file` с
  диском. Образец — scratchpad сессий 12.09 и 14.09 (`ingest_gen.py`, `ingest2_gen.py`).
- **Легаси-таблицы Django-эпохи принадлежат `postgres`:** `ALTER` на них —
  `db=${DATABASE_URL##*/}; sudo -n -u postgres psql -d "${db%%\?*}" -f /tmp/<файл>`.
- **Миграции:** guard роняет авто-деплой → применить psql → `gh workflow run deploy-prod.yml --ref main`.
- **`gh`/`git` рвутся TLS-таймаутом** — всё сетевое в цикл повторов с `timeout`; `git push`
  может зависнуть на 3 минуты, ветка при этом уже на origin — проверять `git ls-remote`.
  Мерж проверять `gh pr view N --json state`. Авто-мерж не включён — `gh pr merge --squash` по зелёному.
- **Деплой не запускается** на изменения только в `docs/`, `mailbox/`, `*.md`, `.github/`,
  `.claude/` (`paths-ignore`) — правка workflow проверяется следующим кодовым выкатом.
- **Текст в команды — файлом (D-046):** commit `-F`, `gh pr create --body-file`; heredoc —
  запрет, а не привычка.
- **Фоновые цепочки без `git checkout`** (см. память): `checkout`/`pull` — только в foreground.
- **`npm audit fix --omit=dev` сносит dev-пакеты** — после него `npm install`.
- **Локальной базы нет** — проверять сборкой, тестами, гейтом; страница входа доступна в
  превью `.claude/launch.json` (порт 3100) и без БД.
- **`main` защищён:** required check `gates`, PR обязателен, `enforce_admins`.
- **Бэкап vault:** cron 03:30, `scripts/backup_vault.sh`; `BACKUP_MEDIA_ROOT` = `MEDIA_ROOT`.
