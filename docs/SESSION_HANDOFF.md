# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-07-17
**Branch:** main
**Прод:** Бокс 1 (`831d0ce99bdf.vps.myjino.ru`, PG16, app :3002), HEAD main `3035a0d` задеплоен,
health 200. `VAULT_PROVISION_KEY` выставлен в `/etc/karman/karman.env`.

---

## Текущая нитка

_Нет — мандат brain 07-12 (self-serve provisioning) закрыт целиком за сессию, эскалация 07-17
снята, ack отправлен._ Попутно: прод-чистка (provsmoke, мёртвый tg-аккаунт) и восстановление
доступа (сброс/смена паролей) — всё в проде.

## Следующий шаг

Нитки нет — работа по запросу владельца. Кандидаты — из `docs/PENDING_FOLLOWUPS.md`
(наполнение личной ячейки, квартальный самоосмотр Q3, deadcode-дельта ~август).

## Контекст

- **Связанные коммиты сессии:** `f223c5d` (#68 self-serve provisioning под
  `VAULT_PROVISION_KEY`), `ca5b7c1` (#69 ack brain + верификация комнаты Казанской),
  `fbbeac6` (#70 чистка хвостов), `3035a0d` (#71 восстановление доступа: панель
  «Пользователи» со сбросом пароля, смена своего пароля, подсказка на /login).
- **Открытые PR:** нет (кроме этого handoff-PR).
- **Открытые вопросы для пользователя:** нет. Временный пароль `Chaka` выдан владельцу
  в чате 07-17 (вход проверен, 200) — человек должен сменить его в «Настройках».

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md`. Витрина: наполнить личную ячейку владельца;
живой TG-тест кнопки напоминания; G54-чек «По датам»; квартальный самоосмотр Q3 2026 (авг–сен);
deadcode-дельта ~август.

## Прод-инфра (для следующей сессии)

- **ssh на Бокс 1 ЕСТЬ:** алиасы `karman` и `GONBA` (ключ `id_ed25519_gonba_deploy`;
  алиас `karman` починен 07-17 — раньше вёл на мёртвый tiny). HTTP с PC40 к боксам режется —
  прод-эндпойнты проверять curl'ом с самого бокса. Деструктив на прод-БД — только с
  подтверждением владельца в том же ходе (#025).
- **Грабля ssh+psql:** строки с `$` (django-хэши!) в двойных кавычках через ssh раскрываются
  удалённым шеллом — слать SQL через stdin (`ssh karman '… psql "$DATABASE_URL"' < file.sql`).
- **Self-serve provisioning:** `POST /api/secrets/provision`, гейт `VAULT_PROVISION_KEY`
  (в karman.env на боксе, brain заберёт сам). Контракт — `docs/secrets-client-guide.md`.
- **Бэкап vault:** cron 03:30, `/home/valstan/karman/scripts/backup_vault.sh`; 2FA владельца
  включена (claim `mfa` для /secrets).
