# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Обновляется в том же PR, что и шаг
> работы (D-066); историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-03
**Branch:** main
**Прод:** задеплоен `b429318` (прогон 33543072088, зелёный), оба сервиса живы. Миграций
эта сессия не приносит. Координаты прода — в **secrets**, в vars только два порта.

---

## Текущая нитка

Сессия 03.09 разбирает четыре письма brain от 02.09. **Первый PR** (этот): D-066 — `/obriv`
удалён, хук `SessionStart` печатает handoff и git-состояние; аудит инструкций под Fable 5.1
применён (пин модели снят, потолки и история-как-правило убраны, D-046 вписан в канон);
`close_session` переписан как страховка, merge — по зелёному без вопроса владельцу.

**Приёмка D-061 сделана прогоном:** аудит комнаты 5 показал три паспортные сессии Сабантуя
01.09 (19:23 рукопожатие; 19:41 и 20:21 — `session_open → push 5 ключей → session_revoked`),
актор `passport:Valstan/SabantuyMalmyzh`, ни одного `401`/`403`. Письмо brain об этом — во
втором PR вместе с grant-endpoint.

## Следующий шаг

1. **D-061 второй ход (mandate, high): grant без сессии владельца.** `POST /api/secrets/grants`
   `{key, target_slug, alias?, note}` под токеном комнаты-**источника** (только `can_write`),
   область — только её секреты; принципал в аудите `room:<slug>`, «основание» обязательное.
   Плюс `GET` (список выданного) и `DELETE` (отзыв) тем же токеном. Сервис — вынести ядро
   `createGrant` в общий insert, роут по образцу `app/api/secrets/self/route.ts`.
2. **Гайд `docs/secrets-client-guide.md`:** раздел про grant по токену + два предупреждения от
   Сабантуя (`::add-mask::` для токена из `/session` **до** первого использования; второй
   `trap … EXIT` молча заменяет первый) + заметка про `tokenPrefix` в открытом логе.
3. **Письмо brain по D-061:** строка аудита есть; grant-endpoint сделан; предупреждения в гайде;
   **два зависших rw-токена** (`dkmalmyzh` id 7, `kalininocks` id 8, выпущены 29.06, ни разу
   не использованы) — решение владельца 03.09: отдать brain, пусть проекты заберут через него.
4. Затем — веха 2 паспорта + GUI привязок ЕСА; R1 (тесты сервисного слоя, задача Q4).

## Контекст

- **Планы и контракты:** `docs/passport-server.md`, `docs/secrets-client-guide.md`,
  `docs/esa-login.md`.
- **Точка возврата слоя инструкций:** тег `pre-fable-freeze-2026-09-02` (= `2bf7b87`).
- **Открытые PR:** этот (chore/d066-fable-instruction-audit).
- **Письма brain этой сессии:** `2026-09-03-instruction-audit-applied-karman`.
- **Открытые вопросы для владельца:** круг ЕСА браузером; где единственная копия закрытого
  gpg-ключа и `SECRETS_MASTER_KEY`; три вопроса из Q3 у brain.

## Failed approaches (этой нитки)

- **Считать, что D-046 «не дошёл», потому что его нет в `AGENTS.md`.** Письмо-broadcast от
  25.08 в ящике лежало; недоставлено было не письмо, а перенос правила в канон. Проверять по
  ящику, а не по канону.

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md`. Витрина: **круг ЕСА браузером владельца**
(чужой ход); **GUI привязок ЕСА + веха 2 паспорта** одним разделом; **R1 — сервисный слой без
тестов** (задача Q4); **чужие Server Action ID в журнале прода** (подтверждение R5); **R5 — прод
без наблюдения**; **migration-guard защищает один push, а не инвариант**; deadcode — октябрь.

## Прод-инфра (для следующей сессии)

- **ssh:** алиасы `karman` и `GONBA`. Первый коннект в сессии может отвалиться по таймауту
  (03.09 так и было) — повторить, прежде чем считать бокс недоступным. Деструктив по прод-БД —
  только с подтверждением владельца в том же ходе (#025).
- **Свежая машина требует `npm ci`** — без него `npm run deadcode` врёт «knip не найден».
- **psql по ssh:** `$DATABASE_URL` в env юзера нет, путь к env-файлу — из юнита
  (`systemctl show karman -p EnvironmentFiles --value`). Рабочая форма:
  `ssh karman 'set -a; . <env-файл>; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f -'`, SQL — через
  stdin, `\pset pager off` первой строкой. Колонки аудита: `at`, `action`, `detail`, `actor`.
- **Логи Actions публичны, `vars` в них не маскируются.** Recon-чувствительное — только в
  `secrets`. Новый шаг с дампом проверять по НАСТОЯЩЕМУ выводу с пустым грепом.
- **Внешние URL проверять С БОКСА** — с рабочей машины исходящий HTTPS идёт через прокси.
- **`main` защищён:** required check `gates`, PR обязателен, `enforce_admins`. Мерж после
  зелёного CI: `gh pr merge <N> --squash --delete-branch` (без `--admin`).
- **Миграции:** `deploy-prod.yml` блокирует авто-деплой на коммите с новой миграцией →
  применить psql'ом, затем `gh workflow run deploy-prod.yml --ref main`.
- **Деплой возит `scripts/`** на бокс. systemd-юниты — шаблоны, рендер `scripts/render_unit.sh`.
- **Бэкап vault:** cron 03:30, `scripts/backup_vault.sh`; список таблиц догоняет схему вручную.
