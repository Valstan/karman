# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-06-05
**Branch:** feat/calc-and-csv-export (PR с кодом — ждёт ручного merge)
**Прод:** `059f1f1` (#11) на `4ce93c2b59f9.vps.myjino.ru`, `karman.service` активен.
Новые фичи этой сессии (калькулятор, экспорт CSV) **ещё НЕ задеплоены** — прод на коммит позади.

---

## Текущая нитка

_Нет — задачи сессии закрыты._ За сессию (на машине без Docker, через общий локальный
Postgres на порту 5433) отгружены две фичи + фикс + актуализация доков, всё проверено вживую
в браузере на сид-данных:

1. **Калькулятор досрочного погашения** — what-if на странице кредита (экономия на процентах +
   сокращение срока при доплате/разовом погашении). Чистая логика `lib/services/early-repayment.ts`.
2. **Экспорт CSV** — кредиты, график платежей, документы. Авторизованные роуты `/api/export/*`,
   CSV под русский Excel (`lib/csv.ts`).
3. **Фикс `bootstrap.sql`** — локальный сид отстал от схемы (не было таблицы категорий
   документов), из-за чего `/documents` падала на свежей локальной БД.

## Следующий шаг

1. **Смержить PR** ветки `feat/calc-and-csv-export` после ревью (в нём код — авто-merge НЕ
   делался): `gh pr merge <N> --squash --delete-branch`, затем `git checkout main && git pull --ff-only`.
2. **Задеплоить на прод** (фичи ещё не в проде): `ssh karman 'cd karman && bash scripts/deploy.sh'`.
   Миграций нет — обычный build + restart (новые колонки/таблицы НЕ добавлялись, bootstrap.sql
   только локальный).
3. Дальше — новая фича по выбору (идеи в «Не забыть»).

## Контекст

- **План:** отдельного файла нет.
- **Связанные коммиты сессии (ветка `feat/calc-and-csv-export`):** docs-актуализация;
  `feat(credits)` калькулятор; `fix(bootstrap)` категории документов; `feat(export)` CSV.
- **Открытые PR:** PR этой ветки (код — калькулятор + экспорт + фикс сида + доки + письмо brain).
- **Открытые вопросы для пользователя:** нет.

## Прод-инфра (как устроено сейчас)

- Сервис: `karman.service` (systemd, `User=valstan`) → `node .next/standalone/server.js` на
  `127.0.0.1:3000`. Env — `/etc/karman.env`. nginx → :3000. `WorkingDirectory=/home/valstan/karman`.
- **Сканы документов:** на ФС в `MEDIA_ROOT` (дефолт `<cwd>/media`, gitignored). Отдача —
  через авторизованный `/api/documents/[id]/file/[slot]`. Каталог `media/` требует бэкапа.
- **SSH-деплой:** алиас `ssh karman`, ключ `~/.ssh/id_ed25519_karman_deploy`, passwordless sudo.
  Деплой: `ssh karman 'cd karman && bash scripts/deploy.sh'`.

## Локальная разработка на ЭТОЙ машине (важно)

- **Docker не установлен.** docker-compose (порт 5432) тут не работает.
- Используется **системный PostgreSQL 17 на порту 5433**: роль/БД `karman`/`karman_db`,
  `bootstrap.sql` уже применён. `.env.local` указывает на `...@localhost:5433/karman_db`
  (файл gitignored, машинно-локальный). Пароль суперпользователя postgres — в `.env`
  соседних проектов (Gonba/MatricaRMZ) на той же машине.
- Запуск: `npm run dev` (или preview на :3100). Вход: admin / admin123.
- Сброс сид-данных: `DROP SCHEMA public CASCADE; CREATE SCHEMA public AUTHORIZATION karman` от
  postgres, затем `bootstrap.sql` от karman.

## Не забыть (low-priority)

- **Калькулятор и экспорт ещё НЕ в проде** — задеплоить после merge (см. «Следующий шаг»).
- Идеи для следующих фич:
  - **Множественные доп. файлы:** `additional_files` сейчас один `varchar(100)` — для нескольких
    нужна отдельная таблица или JSON-колонка (миграция Django-схемы, аккуратно с прод-БД).
  - **Превью-миниатюры** сканов в таблице документов вместо иконок-ссылок.
  - **Серверный поиск/пагинация** — когда записей станет много (сейчас преждевременно).
- Калькулятор — модель аннуитета; для дифференцированных кредитов считает приближённо
  (фиксированный платёж = ближайший взнос), о чём в карточке есть дисклеймер.
