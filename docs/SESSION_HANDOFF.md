# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-08-03
**Branch:** main
**Прод:** Бокс 1 (`831d0ce99bdf.vps.myjino.ru`, PG16, app :3002). Задеплоен `1fa103e`,
четыре деплоя сессии зелёные, смоук `health=200` / `root=307`, юнит `active`.
Миграций в сессии не было. В vault 12 комнат; комнате `vmalmyzhe` выдан первый rw-токен.

---

## Текущая нитка

_Нет — пачка из шести писем brain (29.07–02.08) разобрана целиком за сессию,
все четыре мандата закрыты, ответ отправлен (`mailbox/to-brain/2026-08-03-mandate-batch-closed.md`)._

## Следующий шаг

Работа по запросу владельца. В очереди само:

- **Курьер токена `vmalmyzhe`** — ход владельца, не наш: значение первого rw-токена
  (префикс `skm_QQjtIuuM`) отдано файлом, его нужно положить в `SECRETS_TOKEN` портала
  вМалмыже. Дальше цепочка замыкается без правок: портал зеркалирует
  `GATEWAY_KEY_VMALMYZHE`, setka читает по grant id 2. Проверка у соседа:
  `GET /api/secrets?key=VMALMYZHE_INGEST_KEY` токеном setka перестаёт отдавать `404`.
- **Квартальный самоосмотр Q3 2026** — всплыл по ритму, окно авг–сен открыто, ни разу
  не проводился. По директиве brain 06-10 это **отдельная сессия** (письмо to-brain
  с рефакторинг-предложениями и фич-идеями). Нужен go владельца.
- **deadcode-гигиена** — ритм месяц от 2026-07-10, следующий прогон после 08-10
  (`npm run deadcode`).
- **Волна 2 ADR-0012 (паспортный вход)** — запаркована до зелёной волны 1 (второй
  читатель vault в коде; сегодня читатель один — trener). Probe уже сделан, форма
  выбрана; спека — ADR-0012 §5. Машинный grant в план **не** включён.

## Контекст

- **Связанные коммиты сессии:** `1e788d7` (#81 — ADR-0011: канон `AGENTS.md`, адаптеры
  `CLAUDE.md`/`GEMINI.md`, `.gitignore`), `46085b4` (#82 — подпись автора в подвале),
  `db74690` (#83 — ревизия гейтов #104: `vitest.config.ts`, `tsconfig.json`, `docs/gates.md`),
  `1fa103e` (#85 — фикс ложной красноты теста IV), `a3a2ea5` (#86 — письмо brain + ре-триаж).
- **План:** — (опись гейтов — `docs/gates.md`, канон агентов — `AGENTS.md`).
- **Открытые PR:** нет. #84 закрыт как заменённый на #86 (ветка была отребейзена).
- **Открытые вопросы для пользователя:** перенести токен `vmalmyzhe` в портал; дать (или
  не дать) go на квартальный самоосмотр. Файл с токеном лежит во временной папке сессии —
  удалить после переноса.

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md`. Витрина: **migration-guard защищает один
push, а не инвариант** (тех-долг, идея #099 мозга; единственная дыра, которую ревизия гейтов
осознанно не закрыла); бэкап `media/` на проде — единственный пункт с риском потери данных,
поднять владельцу; квартальный самоосмотр Q3; deadcode после 08-10.

## Прод-инфра (для следующей сессии)

- **ssh на Бокс 1 есть:** алиасы `karman` и `GONBA` (ключ `id_ed25519_gonba_deploy`).
  Деструктив на прод-БД — только с подтверждением владельца в том же ходе (#025).
- **Грабля psql по ssh:** `$DATABASE_URL` в env юзера НЕТ — переменные в
  `/etc/karman/karman.env`. Рабочая форма:
  `ssh karman 'set -a; . /etc/karman/karman.env; psql "$DATABASE_URL" -c "…"'`.
  Строки с `$` в двойных кавычках раскрывает удалённый шелл — литералы слать как `$$…$$`
  или SQL целиком через stdin. Колонки аудита: `at`, `action`, `detail` (не `created_at`/`details`).
- **Внешние URL проверять С БОКСА, а не с PC40:** с рабочей машины исходящий HTTPS идёт
  через прокси и таймаутит (портфолио выглядело мёртвым, с бокса — `200` за 0.06 с).
  С бокса наружу открыто всё, что пробовали (включая `token.actions.githubusercontent.com`).
- **`main` защищён технически:** required check `gates`, PR обязателен, `enforce_admins: true`,
  force-push и удаление ветки запрещены. Прямой push отклоняется даже у владельца (`GH006`).
  Auto-merge в настройках репо выключен — мержить после зелёного CI командой
  `gh pr merge <N> --squash --delete-branch` (без `--admin`).
- **Миграции:** `deploy-prod.yml` блокирует авто-деплой на коммите с новой миграцией →
  применить psql'ом с машины, затем `gh workflow run deploy-prod.yml --ref main`. Guard
  видит только последний коммит (тех-долг выше).
- **Vault:** `POST /api/secrets/provision` под `VAULT_PROVISION_KEY` — заводит комнату И
  выдаёт первый токен нетронутой комнате (ADR-0010). Выдача доступа между комнатами (grant) —
  GUI владельца под 2FA, машинного grant не будет. Бэкап vault: cron 03:30, `scripts/backup_vault.sh`.
