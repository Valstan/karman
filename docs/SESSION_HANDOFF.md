# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-07-28
**Branch:** main
**Прод:** Бокс 1 (`831d0ce99bdf.vps.myjino.ru`, PG16, app :3002). Задеплоен `1af1ef1`
(коммиты после него — доки и `.gitignore`, прод-артефакт не трогают). Миграция `0006`
применена, health 200, в vault 12 комнат.

---

## Текущая нитка

_Нет — мандат brain 07-26 закрыт со своей стороны за сессию._ Разовый перенос
`vmalmyzhe → setka` выполнить было нечего (комната-источник пуста, портал не зеркалировал
`GATEWAY_KEY_VMALMYZHE`), а структурный пробел закрыт grant-примитивом: выдача доступа к
ключу между комнатами вместо копирования — в проде и проверена живым round-trip'ом.

## Следующий шаг

Работа по запросу владельца. Первое, что «само» придёт в очередь:
- **Выдача `vmalmyzhe → setka`** — как только портал зеркалирует `GATEWAY_KEY_VMALMYZHE`
  (probe: `ssh karman` → `psql "$DATABASE_URL"` → `select * from secrets_item i join
  secrets_project p on p.id=i.project_id where p.slug='vmalmyzhe'`). Дальше — один клик в
  `/secrets/6` → «Общие ключи» → «Выдать доступ» (`GATEWAY_KEY_VMALMYZHE` → комната `setka`,
  имя `VMALMYZHE_INGEST_KEY`, основание «мандат brain 2026-07-26») → ack мозгу. Спросил
  brain, оформлять ли выдачу заранее — она умеет ждать ключ.
- **Квартальный самоосмотр Q3 2026** (авг–сен) — окно наступает, помечен `recurring`.

## Контекст

- **Связанные коммиты сессии:** `045fd6f` (#73 grant-примитив: миграция `0006`, `lib/secrets/
  grant.ts`, UI «Общие ключи», доки), `2ceff67` (#74 амендмент #033 + три письма brain),
  `1af1ef1` (#75 живая проверка на проде + уборка комнат), `2ff53bc` (#76 зеркало Codex в
  `.gitignore`).
- **План:** — (дизайн описан в `mailbox/to-brain/2026-07-28-cross-room-grant-design.md`,
  модель — `docs/secrets-manager.md` → «Общие ключи»).
- **Открытые PR:** нет (кроме этого handoff-PR).
- **Открытые вопросы для пользователя:** нет. На будущее: зеркало Codex (`AGENTS.md`,
  `.agents/`, `.codex/`) лежит вне git — если Codex нужен на репо постоянно, стоит
  генерировать его из `CLAUDE.md` при старте сессии, чтобы копии не расходились.

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md` (механика #033 переписана: возраст из даты,
`parked` с проверяемым `until:`, `recurring`, секция «Чужой ход»). Витрина:
**migration-guard защищает один push, а не инвариант** (новый тех-долг, грабля этой сессии);
бэкап `media/` на проде — единственный пункт с риском потери данных, поднять владельцу;
квартальный самоосмотр Q3; deadcode-дельта ~август.

## Прод-инфра (для следующей сессии)

- **ssh на Бокс 1 есть:** алиасы `karman` и `GONBA` (ключ `id_ed25519_gonba_deploy`).
  Деструктив на прод-БД — только с подтверждением владельца в том же ходе (#025).
- **HTTP снаружи:** прод-эндпойнты проверять curl'ом **с бокса**; но веб-UI из браузера на
  PC40 открывается нормально — живые GUI-проверки делаются через Chrome (вход владельца:
  пароль + 2FA вводит он сам).
- **Грабля ssh+psql:** строки с `$` в двойных кавычках раскрываются удалённым шеллом — слать
  SQL через stdin (`ssh karman '… psql "$DATABASE_URL" -f -' < file.sql`).
- **Миграции:** `deploy-prod.yml` блокирует авто-деплой на коммите с новой миграцией →
  применить psql'ом с машины, затем `gh workflow run deploy-prod.yml --ref main`. Осторожно:
  guard видит только последний коммит (см. тех-долг выше).
- **Vault:** `POST /api/secrets/provision` под `VAULT_PROVISION_KEY`; выдача доступа между
  комнатами — только GUI владельца под 2FA. Контракт клиентов — `docs/secrets-client-guide.md`.
  Бэкап vault: cron 03:30, `scripts/backup_vault.sh`.
