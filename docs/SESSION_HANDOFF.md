# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-06-28
**Branch:** main
**Прод:** **Бокс 1** (`831d0ce99bdf.vps.myjino.ru`, PostgreSQL 16.14, app :3002),
крутит `b72023d` (Zod-фикс включён). `karman` + `karman-reminders` = active, воркер —
единственный поллер Telegram. Старый tiny **удалён владельцем** (откат закрыт).

---

## Текущая нитка

**Backlog-сессия (2026-06-28): разбор отложенного, 4 PR в проде.** После доклада о `/payments`
(#42, хвост #41 — brain уведомлён `mailbox/to-brain/2026-06-28-payments-page-delivered.md`)
прошлись по `PENDING_FOLLOWUPS.md` в порядке «делается мной автономно»:
- **#43 ESLint lint-гейт** — flat-config `eslint-config-next` 16 на ESLint 9, `eslint . --max-warnings 0`,
  шаг в CI (гейтов теперь **4**: typecheck/lint/test/build). Единственное предупреждение устранено
  по существу (watch→useWatch/getValues в форме напоминаний). Пробел, что отмечал brain, закрыт.
- **#44 Превью-миниатюры сканов** — таблица документов рисует `<img>`-миниатюру для растровых
  файлов, иконку для PDF (`isImagePath` + per-slot флаги, пути на клиент по-прежнему не уходят).
- **#45 Календарь будущих срабатываний** — в форме напоминания live-блок «Ближайшие срабатывания»
  (`nextFires` + `formValuesToSpec`, зеркало серверного `buildSpec`; сервер — источник правды).

Задача «произвольные даты» переформулирована: это контрактное изменение, требует живого
**G54 round-trip** владельца → отдельная фокус-сессия (в backlog с пометкой).

**KARMAN по-прежнему между нитками.** Катовер tiny → Бокс 1 закрыт (Ф3–Ф8); хвост — живой
TG-тест кнопки (за владельцем). Новых директив brain нет. Сессия продолжается (разбор backlog).

## Следующий шаг

1. **Продолжаем backlog** — оставшееся в основном gated (см. `PENDING_FOLLOWUPS.md`): произвольные
   даты (live round-trip владельца), множественные доп.файлы + удалить reminder id1 (прод-миграция
   через brain), дайджест/P5/AI-дашборд (owner-gated), мульти-провайдер монитор (кандидат на выброс).
2. **Живой TG-тест кнопки (действие владельца)** — нажать кнопку/команду боту → проверить
   ingest+write-back на Боксе 1 → письмо brain → катовер 100%.
3. **Будущие прод-миграции на Боксе 1 — через brain** (RACI playbook §1, 2026-06-16): готовлю SQL
   по migration-guard-паттерну → brain применяет psql'ом. **ssh к Боксу 1 у меня нет**, локальный
   `ssh karman` указывает на УДАЛЁННЫЙ tiny — не использовать; живость — HTTP-проба
   `https://831d0ce99bdf.vps.myjino.ru/api/health`. См. память `karman-prod-box1-no-ssh`.

## Контекст
- **План:** нет.
- **Связанные PR сессии:** #42 (хвост payments + handoff), #43 (ESLint-гейт), #44 (миниатюры сканов), #45 (календарь срабатываний). Все смержены + задеплоены, прод 200.
- **Открытые PR:** нет.
- **Открытые вопросы для пользователя:** живой TG-тест кнопки (физическое действие владельца, см. Следующий шаг).

## Failed approaches (этой нитки)

- **ssh на Бокс 1 локальным ключом (`id_ed25519_karman_deploy`)** — `Permission denied (publickey)`.
  Доступ к боксу за brain (CI-деплой-ключ); ключ на общий бокс не добавляют сознательно
  (минимизация key-surface, per-app-изоляция — отдельный шаг по слову владельца). Прод-ops веду
  через brain. Локальный `~/.ssh/config` алиас `karman` ещё на мёртвый tiny — для проверки прода
  не использовать, только HTTP-проба.

## Не забыть (low-priority)

Канонический список с метками — `docs/PENDING_FOLLOWUPS.md`. Витрина: живой TG-тест кнопки на
Боксе 1 (владелец); удалить тестовое напоминание id 1 на Боксе 1 (через brain — у меня нет DB-доступа);
P3-хвосты напоминаний (праздники / cron / произвольные даты — календарь UI сделан #45); P5
напоминаний (богатый контент); бэкап `media/` на Боксе 1; квартальный самоосмотр Q3 2026.
