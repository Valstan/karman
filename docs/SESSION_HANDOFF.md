# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю (в т.ч. подробности 11–12.09: `karman-tg`, G331, D-078, карта
> #141, документы семьи #143–#145) смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-09-14
**Branch:** `main`
**Прод:** код `d95ff1d` (#152), `next` 16.3.5, миграции `0014`–`0017` применены. На 14.09:
юнит `active`, `health=200`; в env сервиса `MEDIA_ROOT` и `BRAIN_MAP_GITHUB_TOKEN`
(`ECOSYSTEM_MAP_FILE` убран, файл `ecosystem-map.json` с бокса удалён). В `media/` 115 сканов.

---

## Текущая нитка

_Нет — последняя задача закрыта._ Сессия 13–14.09 сделала четыре шага, все на проде:

| Что | PR |
|---|---|
| **D-090** — «Карта проектов» со вкладками из `docs/map/` репо Мозга: GitHub Contents API, read-only PAT владельца, кэш 10 мин + «Обновить сейчас», при недоступном GitHub последняя удачная версия с пометкой; `react-markdown` + `remark-gfm` с `skipHtml`; `/map?tab=<slug>`. Старая карта #141 снята. Токен создал и вписал в env владелец сам (PAT выпускается только в его аккаунте GitHub). Отчёты Мозгу отправлены (D-086 закрыт той же строкой) | #147, #148, #149 |
| Рекомендация Мозга 12.09: строка `project_deleted` в аудит секретов **до** каскадного `DELETE`, в одной транзакции (`project_id = NULL`, `slug=… audit=N tokens=M items=K`, `owner:<id>`). Прогон владельцем на проде (комната `smoke-del`, строка аудита id 1238), строка Мозгу отправлена | #150, #151 |
| **Догрузка документов семьи** из `D:\valstan\karman-inbox` (140 файлов, 45 новых по md5, 34 загружено, 9 пропущено как повторные фото одного разворота): +5 документов (три свидетельства владельца об обучении/удостоверение кинодемонстратора, диплом переподготовки Ольги 2019, свидетельство о рождении Ярослава), военный билет владельца — 13 полей и 10 страниц, две пометки «уточнить по скану» сняты (серия свидетельства Ольги I-ИР 462761; квалификация в дипломе колледжа — педагог-организатор хореографического коллектива, рег. № 2886). Итого в `admin`: **64 документа, 115 сканов, 375 полей**. Скрипты — в scratchpad сессии, в git не попали (персональные данные) | (данные, без PR) |
| Шаблон «Военный билет»: +«Воинская часть и период службы», «Служба в запасе», «Группа крови» | #152 |

## Следующий шаг

Открытой нитки нет. Кандидаты по приоритету:

1. **Дайджест Мозга 12.09** (ack none) — наше: **#312** `npm audit` по расписанию, а не только
   в PR (два critical RCE в `next` жили на проде между PR); **G340** — проверить, что на
   страницах после входа нет Вебвизора/метрики (`grep -rn "mc.yandex\|ym(" app components`).
2. **Смоук владельца глазами** по #145 (карточки людей в `/documents`, «Собрать выборку» →
   zip, тёмная тема) — всё ещё не подтверждён. Единственная старая пометка «уточнить по
   скану»: дата постановки на учёт в приписном Даниила (id 95).
3. Далее — `docs/PENDING_FOLLOWUPS.md`: R1 (тесты инвариантов доступа, Q4), гейт таблиц
   бэкапа, G321. Легаси-cookie — снять после 18.09 (парк в PENDING).

## Контекст

- **Планы и контракты:** `docs/ecosystem-map.md` (карта: источник, токен, поведение),
  `docs/OPERATIONS.md`, `docs/secrets-client-guide.md`, `docs/passport-server.md`,
  `docs/esa-login.md`, `docs/telegram-reminders.md`.
- **Аккаунты прода:** `admin` (id 1, superuser, привязан к ЕСА), `Valstan` (id 17, отключён),
  `Chaka` (id 29). Документы семьи — все в `admin`, различаются полем `holder`.
- **Комнаты секретов:** 11. Личности паспорта: 1 trener, 2 Gonba, 3 Sabantuy, 4 Kazanskaya,
  5 vMalmyzhe, 6 DKMalmyzh.
- **Открытые PR:** нет. **Открытые вопросы для владельца:** где единственная копия
  закрытого gpg-ключа и `SECRETS_MASTER_KEY` (висит с августа).
- **Почта brain:** все письма по 12.09 включительно обработаны; отправлены 14.09 три отчёта
  (`mailbox/to-brain/2026-09-14-*`). Новых входящих на вечер 14.09 не проверялось после
  утреннего `/start` — первое дело следующей сессии.

## Не забыть (low-priority)

Канонический список — `docs/PENDING_FOLLOWUPS.md`. Витрина: **смоук владельца**;
**#312 / G340** из дайджеста; **легаси-cookie** после 18.09; **R1**; **G321**; **гейт таблиц
бэкапа**; deadcode — октябрь. Фото для карточек: документ вида «Фотография» у каждого
человека (пока ни одного).

## Прод-инфра (для следующей сессии)

- **ssh:** алиасы `karman` и `GONBA`. Первый коннект может отвалиться — повторить.
  Деструктив по прод-БД — только с подтверждением владельца в том же ходе (#025).
- **Порт приложения — 3002**, в юните. Env-файл сервиса — см. `EnvironmentFile` юнита
  (`/etc/karman` root-only: правки только руками владельца, секреты агент не вписывает).
- **psql по ssh (D-046):** SQL в файл → `ssh karman 'cat > /tmp/<файл>' < <файл>` →
  `ssh karman 'envf=$(systemctl show karman -p EnvironmentFiles --value | cut -d" " -f1); set -a; . "$envf"; psql "$DATABASE_URL" -f /tmp/<файл>'`.
  `\pset pager off` первой строкой. Временные файлы с персональными данными — удалять с бокса.
- **Загрузка документов пачкой:** генератор SQL + tar по slug → на бокс → psql в транзакции
  с `ingest_map` → `mv` в `$MEDIA_ROOT/documents/1/<id>/` → сверка путей `document_file` с
  диском. Образец — scratchpad сессий 12.09 и 14.09 (`ingest_gen.py`, `ingest2_gen.py`).
- **Легаси-таблицы Django-эпохи принадлежат `postgres`:** `ALTER` на них —
  `db=${DATABASE_URL##*/}; sudo -n -u postgres psql -d "${db%%\?*}" -f /tmp/<файл>`.
- **Миграции:** guard роняет авто-деплой → применить psql → `gh workflow run deploy-prod.yml --ref main`.
- **`gh`/`git` рвутся TLS-таймаутом** — всё сетевое в цикл повторов с `timeout`; `git push`
  может зависнуть на 3 минуты, ветка при этом уже на origin — проверять `git ls-remote`.
  Мерж проверять `gh pr view N --json state`. Авто-мерж не включён — `gh pr merge --squash` по зелёному.
- **Фоновые цепочки без `git checkout`** (см. память): `checkout`/`pull` — только в foreground.
- **`npm audit fix --omit=dev` сносит dev-пакеты** — после него `npm install`.
- **Локальной базы нет** — проверять сборкой, тестами, гейтом; страница входа доступна в
  превью `.claude/launch.json` (порт 3100) и без БД.
- **`main` защищён:** required check `gates`, PR обязателен, `enforce_admins`.
- **Бэкап vault:** cron 03:30, `scripts/backup_vault.sh`; `BACKUP_MEDIA_ROOT` = `MEDIA_ROOT`.
