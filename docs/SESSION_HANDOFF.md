# Session Handoff

> Sticky-note для непрерывности между сессиями KARMAN. Перезаписывается через
> `/close_session` — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-06-29
**Branch:** main
**Прод:** **Бокс 1** (`831d0ce99bdf.vps.myjino.ru`, PostgreSQL 16, app :3002), крутит HEAD main —
**менеджер секретов + произвольные даты напоминаний в проде** (2026-06-29). `karman` +
`karman-reminders` = active, воркер — единственный поллер Telegram. Старый tiny удалён.

---

## Текущая нитка

**Большая backlog+feature сессия (2026-06-28…29).** В проде за сессию: payments-хвост (#42),
ESLint-гейт (#43, гейтов теперь 4), миниатюры сканов (#44), календарь срабатываний напоминаний
(#45), произвольные даты напоминаний (#47), **менеджер секретов (#48)**.

- **Менеджер секретов (#48)** — новый модуль: зашифрованное хранилище ключей проектов
  (AES-256-GCM, мастер-ключ в env `SECRETS_MASTER_KEY`), доступ проектов по Bearer-токену
  (`GET /api/secrets`), UI `/secrets` (проекты → секреты + токены + аудит). Владелец одобрил;
  миграцию `0002_secrets_manager` применил САМ через SSH, деплой `workflow_dispatch`, мастер-ключ
  (его выставил brain) подхватился рестартом. Проверено снаружи: health 200, `/api/secrets` → 401
  (таблицы есть, не 500). Runbook — `docs/secrets-manager.md`.
- **SSH к Боксу 1 ПОДТВЕРЖДЁН** (2026-06-29, по слову владельца): `ssh karman` (перенацелен) или
  `ssh GONBA` → `valstan@831d0ce99bdf`. Прежнее «нет SSH» оказалось ошибкой (алиас `karman` смотрел
  на мёртвый tiny). Теперь миграции/деплой делаю сам — почта brain для рутины не нужна.

KARMAN снова **между нитками**. Катовер tiny → Бокс 1 закрыт (Ф3–Ф8). Новых директив brain нет.

## Следующий шаг

1. **Фичи/backlog по запросу владельца.** Остаток в `PENDING_FOLLOWUPS.md` (gated / по календарю).
2. **Прод-операции — self-serve** (см. память `project-prod-box1`):
   - Миграция: `ssh karman 'set -a; . /etc/karman/karman.env; set +a; psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f -' < lib/db/migrations/000X.sql` → проверка `\dt`.
   - Деплой: `gh workflow run deploy-prod.yml` (workflow_dispatch — пропускает migration-guard) или `bash scripts/deploy_remote.sh`. On-box `next build` запрещён.
   - Деструктив на живых данных (`DROP/DELETE/UPDATE/TRUNCATE`, `systemctl stop`) — подтверждение владельца в том же ходе (#025).
3. **Хвосты владельца:** живой TG-тест кнопки напоминания; (опц.) Telegram-чек произвольных дат.

## Контекст
- **План:** нет.
- **Связанные PR сессии:** #42 (payments-хвост), #43 (ESLint), #44 (миниатюры), #45 (календарь),
  #47 (произвольные даты), #48 (менеджер секретов); письма brain #49/#51 (миграция секретов —
  закрыто, применил сам). Всё смержено / в проде.
- **Открытые PR:** нет.
- **Открытые вопросы для пользователя:** живой TG-тест кнопки (физическое действие владельца).

## Failed approaches / исправленные заблуждения

- **«Нет SSH к Боксу 1» — оказалось ОШИБКОЙ (исправлено 2026-06-29).** Доступ ЕСТЬ:
  `ssh karman` / `ssh GONBA` → `valstan@831d0ce99bdf` (ключ `~/.ssh/id_ed25519`, общий бокс).
  Прежний провал — устаревший алиас `karman` (вёл на мёртвый tiny `4ce93c2b59f9`) + попытка
  несанкционированного `id_ed25519_karman_deploy`. `valstan` — НЕ роль в Postgres → для psql
  источать `/etc/karman/karman.env` на боксе (роль БД `karman_app`). Деталь — память `project-prod-box1`.

## Не забыть (low-priority)

Канонический список с метками — `docs/PENDING_FOLLOWUPS.md`. Витрина: живой TG-тест кнопки (владелец);
удалить тестовое напоминание id 1 на Боксе 1 (теперь могу сам по SSH, но это DELETE → подтверждение
владельца #025); P3-хвосты напоминаний (праздники / cron); P5 (богатый контент); бэкап `media/` на
Боксе 1; выделенный karman ssh-ключ (гигиена, сейчас через ключ GONBA); квартальный самоосмотр Q3 2026.
