# Pending followups — отложенное, которое не должно сгнить молча

> Pool #033 (deferred-backlog aging) + **амендмент brain 2026-07-28**. Механика первой
> редакции деградировала молча: рядом с датой стояло слово-статус (`decay: fresh`), с датой
> никак не связанное — дата протухала, слово оставалось нормальным, и `/start` либо молчал,
> либо докладывал протухшим всё подряд. **Слова-статуса больше нет: возраст считается из даты
> `last-touch`.**
>
> Метки пункта: `added` / `last-touch` / `snoozed` (счётчик переносов) и, если применимо,
> `parked … until:` или `recurring … every: / last-run:`.
>
> **Что всплывает на `/start`:**
> - обычный пункт — `last-touch` старше **30 дней** или `snoozed ≥ 3`;
> - `parked` — **только когда выполнилось условие `until:`**. Условие обязано быть проверяемым
>   (события «владелец попросил», «сосед сделал», «данные выросли до X»), иначе это `stale`
>   под другим именем;
> - `recurring` — программа, а не задача (гигиена, аудиты, срезы): её здоровье — ритм `every:`
>   от `last-run:`, возраст записи ни о чём не говорит.
>
> **Пункт про инфраструктуру/прод обязан нести признак проверки** — команду, которую можно
> прогнать за минуту и получить да/нет (`ls`, `crontab -l`, счётчик строк в БД). Ре-триаж без
> её прогона — это перечитывание собственной формулировки, а она устаревает молча: пункт
> «бэкапа `media/` нет» был ложен с 11 июля и пережил три ре-триажа подряд (2026-08-10).
>
> **Исходы ре-триажа — четыре:** возобновить / переформулировать под текущий код / выкинуть
> с причиной / запарковать. Секция «Чужой ход» живёт по своим правилам: наша работа там
> закончена, стареть по возрасту ей нечем. Brain читает этот файл кросс-аудитом (read-only).

## Чужой ход (наша работа закончена, ждём не нас)

- ~~**Казанская и портал заходят по паспорту (D-061, мандат brain 03.09)**~~ — условие
  `until:` выполнено: прогон признака 2026-09-04 — у Казанской (комната 11) пять
  `session_open`, восемь `push`; у портала (комната 6) `session_open` 03.09 15:50 UTC.
  Статический токен портала brain велел **не отзывать** (письмо 03.09 «Откат принят»: им
  работает рантайм-дотяг и зеркало с бокса, намеренно). Закрыто.
- ~~**ДК Малмыжа заходит по паспорту; токен 7 — отозвать после первого `session_open`**~~ —
  условие `until:` выполнено, пункт закрыт **2026-09-04**. Прогон признака: в комнате 7 две
  строки `session_open` с актором `passport:Valstan/DKMalmyzh` (аудит 799 в 08:22 UTC и 805
  в 09:02, за каждой — `pull` ключей и самоотзыв сессии), то есть паспортный путь ДК рабочий.
  Токен 7 отозван в 12:14:09 UTC с подтверждением владельца в том же ходе (#025), строка
  аудита `token_revoked` id **834**, `actor owner:1`. Гейт «ещё не отозван» стоял в `WHERE`
  самого `UPDATE` — повторный прогон файла не переписал бы дату и не задвоил бы аудит.
  Сам токен так и не был использован ни разу (`last_used_at` пуст): сессии шли на паспортных
  токенах 50/51, отзыв ничего живого не сломал. Личность `passport_identity` id 6
  (`repository_id` 1276073365) остаётся. Отписано brain.
- ~~**Двусторонний grant — ждём согласия brain на схему.**~~ — условие `until:` выполнено:
  brain сказал «да» письмом 03.09 «Откат принят», форма — наша. Сделано 04.09 (миграция
  `0014`, `POST /api/secrets/grants` → `pending`, `POST …/grants/<id>/accept` токеном
  получателя, `GET ?pending=1`, отзыв с любой стороны). Закрыто.
- **Первый `accept` от `room:setka` на выдачу Казанской — чужой ход.** Казанская предлагает
  `INGEST_GATEWAY_KEY → setka` как `KAZANSKAYA_INGEST_KEY`, Сарафан принимает по allowlist
  (мандаты brain обоим ушли 03.09). Признак: `select at, action, actor, detail from
  secrets_audit where action in ('grant_proposed','grant_in') and project_id in (4,11)
  order by id desc limit 6` → `grant_in` с актором `room:setka`. Если `grant_proposed` есть,
  а `grant_in` неделю нет — Сарафан не сделал шаг «принять по allowlist», напомнить brain.
  **Ре-триаж 2026-09-04 (прогон признака):** ждём не того шага. Предложения ещё нет:
  `grant_proposed` на проде не писался ни разу, pending-выдач ноль (`accepted_at IS NULL AND
  revoked_at IS NULL` — 0 строк), выдач с источником «комната 11» ноль. То есть первым ходит
  не Сарафан, а **Казанская** — она ключ пока не предложила. Ветка «есть `proposed`, неделю
  нет `grant_in` → напомнить brain» в прежней формулировке не сработала бы никогда, потому
  что её первое условие не наступало. Уточнение к признаку: при `accept` строка `grant_in`
  пишется на комнату-ПОЛУЧАТЕЛЯ, то есть `project_id = 4` с актором `room:setka`.
  `until:` в аудите есть `grant_proposed` от комнаты 11 → дальше ждём `grant_in` от `room:setka`.
  `added: 2026-09-04` `last-touch: 2026-09-04`
- **Смоук эпика «Личные данные и Круг» браузером владельца.** Всё сделано и в проде, но
  вживую не прогонялось ничего: локальной базы на рабочей машине нет, проверено сборкой,
  354 тестами и гейтами маршрутов. Пройти по порядку: карточка в `/profile` → документ по
  шаблону «Паспорт РФ» с двумя файлами → круг, приглашение `Chaka` → согласие под ним →
  `/circle/people` показывает обоих → `/circle/export`: галочки, .txt, .docx, .zip,
  «Печать», «Поделиться». Отдельно **печать**: правка печатных стилей сделана вслепую
  (`app/globals.css`, `@media print`) — убедиться, что на бумагу уходит весь текст, а не
  первая страница, и что интерфейс на лист не попал.
  **Уточнение по данным прода (прогон 2026-09-04):** проверять придётся только новый путь.
  `documents_document` — 0 строк, `document_field` — 0, `document_file` — 0; единственный
  `person_profile` по переносимым реквизитам пуст, поэтому перенос из миграции 0015 создал
  ноль документов. Регрессию «существующие документы стали личными» подтвердить не на чем —
  сценарий сводится к «создать документ по виду → отметить галочкой → `Chaka` видит только
  отмеченное». Справочник категорий на месте (8 штук), выбирать есть из чего.
  `until:` владелец прошёл сценарий. `added: 2026-09-04` `last-touch: 2026-09-04`
- **Живой TG-тест кнопки напоминания** — нужен Telegram владельца: нажать кнопку напоминания /
  команду боту → проверяем ingest+write-back на проде. После — письмо brain (он доклада ждёт) →
  катовер 100%. `until:` владелец нажал кнопку в Telegram.
  `added: 2026-06-16` `last-touch: 2026-07-28`
- **G54-чек «По датам»** (belt-and-suspenders к режиму `repeat:'dates'`, #47 в проде) — владелец
  создаёт напоминание «По датам» → переоткрывает → получает срабатывание в Telegram.
  `until:` владелец прогнал сценарий. `added: 2026-06-28` `last-touch: 2026-07-28`
- ~~**Сквозной смоук паспорта — прогон CI пилота (trener)**~~ — условие `until:` выполнено.
  trener прошёл `POST /api/secrets/session` из своего CI 24.08, **201 с первого прогона**:
  `identity: "Valstan/trener"`, `canWrite:false`, `jwksStale:false` (то есть сессия паспортная,
  а не статический токен, и ключи issuer'а подтянулись живьём). Проверено brain по артефакту
  (`passport-probe.yml` в их `main`), а не по ack. **Гейт волны 2 снят**, веха 2 (GUI реестра
  личностей) разблокирована. Закрыто 2026-08-25.
- ~~**Рассылка примитива «времянка» проектам — ход brain**~~ — условие `until:` выполнено:
  brain разослал (первым получателем — SabantuyMalmyzh, у которого волна 1 внедрена целиком,
  а зеркалирование стояло только из-за невыданного `SECRETS_TOKEN`). Закрыто 2026-08-25.
- ~~**MatricaRMZ обменивает времянку `skb_hq95azWd…`**~~ — условие `until:` выполнено ещё
  2026-08-10 14:37 UTC (строка `claim` в аудите комнаты 2), а пункт пережил это незамеченным
  до прогона признака 2026-09-04: парк с проверяемым условием тоже протухает, если условие
  никто не прогоняет. Закрыто.
- **Provisioning не доказывает доставку токена** — третий случай одного класса (`rmzmalmyzh`
  07-26, `gonba` 08-10, `matricarmz` 08-10): токен выпускается, не доезжает, и комната с
  потерянным токеном неотличима от комнаты, которой он просто не нужен. Признак проверки —
  `select slug from secrets_project p join secrets_token t on t.project_id=p.id where
  t.last_used_at is null and t.revoked_at is null`. Кандидат в лечение: срок жизни у
  НЕиспользованного токена (не использован за N дней — гаснет сам), тогда «не доехал»
  перестаёт быть вечным состоянием. Времянка это уже делает по своей природе.
  **Прогон признака 2026-09-01:** было три строки, осталось две — `dkmalmyzh` (токен 7) и
  `kalininocks` (токен 8), обе rw и бессрочные, выпущены 29.06 и ни разу не использованы.
  Третья (`sabantuymalmyzh`, токен 5) закрыта 09-01 по D-061: отозвана, комната
  переведена на паспорт. Паспортный путь этот класс лечит структурно — сессия живёт час и
  гаснет сама, «не доехал навсегда» становится невозможным состоянием.
  **Решение владельца 2026-09-03:** две старые строки (`dkmalmyzh` токен 7, `kalininocks`
  токен 8) отданы brain письмом `2026-09-03-d061-audit-row-grant-endpoint-two-stuck-tokens`.
  **Ответ brain 03–04.09, исполнено 04.09:** токен 8 отозван (D-074, Калинино закрыт),
  токен 7 ждёт первого `session_open` ДК — это отдельный пункт в «Чужом ходе». Прогон
  признака 2026-09-04 после отзыва: одна строка, токен 7. Класс «не доехал навсегда» лечит
  паспорт структурно; кандидат в лечение для статических токенов (гаснуть без использования
  за N дней) остаётся идеей до второго такого случая после перевода всех на паспорт.
  `parked` `until:` появился новый неиспользованный статический токен старше 30 дней
  (признак выше вернул строку не с id 7). `added: 2026-08-10` `last-touch: 2026-09-04`
- ~~**Сабантуй меняет удостоверение на токен комнаты (D-061)**~~ — условие `until:`
  выполнено: прогон признака 2026-09-03 показал три паспортные сессии 01.09 (19:23
  рукопожатие; 19:41 и 20:21 — `session_open → push 5 ключей → session_revoked`), актор
  `passport:Valstan/SabantuyMalmyzh`, ни одного `401`/`403`. Brain отписан. Закрыто.
- **Раздача vault-токенов проектам** (env `SECRETS_TOKEN` + интеграция save/load на их стороне) —
  owner/project-driven, как было с trener. Выдано: `vmalmyzhe` (08-03), `gonba` (08-10 —
  транзакцией на проде, доставка в секрет репозитория `Valstan/Gonba` через `gh secret set`).
  `until:` проект попросил токен / владелец раздал.
  `added: 2026-06-30` `last-touch: 2026-08-10`

- **Исполняет ли ЕСА `prompt=login` — не проверено.** Привязка ВК просит провайдера
  переспросить человека (`lib/auth/oidc-pkce.ts`, ветка `mode === 'link'`). Если ЕСА параметр
  игнорирует, вернётся личность, уже открытая в браузере, и единственной защитой останется
  экран подтверждения. Это не поломка — двухшаговая привязка на такой случай и сделана, — но
  предпосылка записана как факт, а фактом не является.
  Признак (даёт да/нет за один прогон): владелец жмёт «Привязать аккаунт ВКонтакте», уже
  будучи залогиненным во ВКонтакте. Переспросили логин → исполняет; пустили молча → нет.
  Если нет — записать ограничением в `docs/esa-login.md` и сказать brain (у соседей
  привязка внешних аккаунтов может быть одношаговой).
  `added: 2026-09-04` `parked` `until:` владелец прогнал привязку браузером.
  `last-touch: 2026-09-04`
- **Легаси-имя сессионной cookie дочитывается, но больше не пишется.** 2026-09-04 cookie
  переименованы в `__Host-…` (сосед по `вмалмыже.рф` мог подбросить свою). Чтобы никого не
  разлогинить, `readSessionToken` читает новое имя, при промахе — старое
  (`SESSION_COOKIE_LEGACY`, `TOTP_PENDING_COOKIE_LEGACY`); запись идёт только в новое.
  Через `SESSION_TTL_SECONDS` = 14 дней все живые сессии переедут сами, и обе константы с
  ветками чтения можно убрать. Признак (даёт да/нет за минуту): прошло ли 14 дней с
  выкатки, то есть `git log -1 --format=%cd -- lib/auth/jwt.ts` старше 2026-09-18.
  Раньше срока убирать нельзя — разлогинит тех, кто не заходил.
  `added: 2026-09-04` `parked` `until:` наступило 2026-09-18. `last-touch: 2026-09-04`

## Программы (recurring)

- **#036 deadcode-гигиена: knip + depcheck** — инструмент внедрён, процесс — `docs/deadcode.md`,
  прогон `npm run deadcode` (config `knip.jsonc`). История: первый триаж 06-13 (остатков Vite/
  Express нет — чистая миграция; удалено 5 born-unused); 07-10 — `reminders-worker.mjs` false
  positive (systemd-вход, прописан в entry), удалены `sendPhoto`/`editMessageText` (#24, вернутся
  из git при P5-вложениях) и тип `SecretPushInput`, depcheck чист; 08-10 — удалены
  born-unused типы `SecretCardImportInput` и `SecretProvisionInput`, depcheck чист.
  **Третий рецидив одного класса** (после `SecretPushInput`): в `lib/validation/secret.ts`
  алиасы заводились «по одному на схему» из соображений симметрии, а импортировались не все.
  Причина устранена комментарием у списка алиасов — заводить по факту импорта.
  **09-01 — первый полностью чистый прогон:** knip пуст (код 0), depcheck без замечаний,
  триажить нечего. Рецидива класса born-unused алиасов не случилось — починка причины
  комментарием (а не удалением очередного символа) держится месяц.
  `recurring` `every: месяц` `last-run: 2026-09-01` (следующий — октябрь)
- **Квартальный стратегический самоосмотр** — директива brain 06-10
  (`2026-06-10-deadcode-gate-and-self-review`). **Q3 проведён 2026-08-10**, письмо —
  `mailbox/to-brain/2026-08-10-q3-self-review.md`: замер (19.6k строк, прод 12 МБ, 46 кредитов,
  0 документов, 1 напоминание), пять предложений с ценой, три вопроса владельцу вместо трёх
  фич, один процессный вывод в pool. Главная задача Q4 названа явно — R1 ниже.
  `recurring` `every: квартал` `last-run: 2026-08-10` (следующий — Q4, ноя–дек)

- **R1 (главная задача Q4). Сервисный слой не покрыт тестами — ни один из 17 сервисов,
  ходящих в БД.** Тесты в `lib/services/` есть, но ровно два и оба на чистые калькуляторы
  (`schedule.test.ts`, `early-repayment.test.ts`) — глоб `lib/services/*` не признак, считать
  надо модули с БД. У них ноль тестов, включая `secrets.ts` (1300 строк), где живёт вся
  авторизация доступа к чужим секретам (владение комнатой, срок и отзыв токена, заслонение
  grant'ов, запрет перезаписи выданного имени). Причина структурная: нет харнесса с БД.
  План: эфемерный Postgres (`pglite` или контейнер в CI) — ~1 сессия, затем ~15 тестов
  **на инварианты доступа, а не на CRUD** — ~1 сессия.
  Блокирует R3 (резать `secrets.ts` без тестов — рефакторинг вслепую).
  Признак проверки: `ls lib/services/*.ts | grep -v test | wc -l` минус два чистых модуля.
  `added: 2026-08-10` `snoozed: 0` `last-touch: 2026-09-03`

- **Чужие Server Action ID в журнале прода — признак сканирования, замеченный вручную.**
  Попался при разборе дампа журнала 2026-09-01: `Error: The Server Reference ID did not match
  the expected format. Received "x"` — 57 раз за неделю, всплеск 27.08 (49 за день).
  Само по себе **не дыра**: это Next.js отвергает битый идентификатор, то есть защита
  сработала. Тревожно другое — **узнали случайно**, глазами, потому что чинили соседнее.
  Признак проверки: `ssh karman 'sudo journalctl -u karman --since "7 days ago" -o cat |
  grep -c "Server Reference ID"'`. Это конкретное подтверждение R5 ниже: пока наблюдения нет,
  всплеск в пятьдесят попыток за сутки не виден никому.
  `added: 2026-09-01` `snoozed: 0` `last-touch: 2026-09-01`
- **R5. Прод без наблюдения** — единственная запись в cron бокса это бэкап: ни health-чека,
  ни алерта «приложение легло», ни проверки, что жив воркер напоминаний. Падение заметит
  владелец, когда откроет страницу. Транспорт оповещения уже построен (Telegram-бот
  напоминаний), поэтому watchdog = `curl /api/health` + сообщение тем же ботом. ~0.5 сессии.
  **Ре-триаж 2026-09-04 (всплыл по возрасту):** возобновлён и **сделан в тот же день**.
  `scripts/health_watch.sh` + cron каждые 10 минут: четыре признака (оба юнита активны,
  `/api/health` = 200 с телом `ok`, лог бэкапа свежее 36 ч), сообщение в Telegram владельцу
  только при СМЕНЕ состояния. `chat_id` кэшируется на диск на каждом прогоне — при мёртвом
  Postgres разрешить получателя уже нельзя, а это ровно тот случай, ради которого сторож и
  нужен (первая редакция звала разрешение только из отправки, кэш оказался пуст — поймано
  прогоном, не рассуждением). Поправка к прежней формулировке пункта: `/api/health` не
  «просто 200» — он ходит в БД (`SELECT 1`) и на её отказе отдаёт 500.
  Признак: `ssh karman 'crontab -l'` — две строки (бэкап + сторож).
  **Остаток:** тревога ни разу не срабатывала по-настоящему — учебный прогон висит
  отдельным пунктом ниже, того же класса, что непрогнанное восстановление бэкапа.
  `added: 2026-08-10` `snoozed: 1` `last-touch: 2026-09-04`
- ~~**Учебная тревога сторожа ни разу не прогонялась**~~ — **прогнана 2026-09-04**, боевым
  путём и с согласия владельца. `karman-reminders` остановлен в 13:56 UTC; cron-прогон
  сторожа в 14:00:01 распознал `не активен (inactive)` и доставил сообщение в Telegram
  **с первой попытки** (реле живо, повторы G307 не понадобились); юнит поднят, прогон
  14:10:01 зафиксировал `состояние восстановилось` и прислал второе сообщение. То есть
  проверены все четыре звена: cron → обнаружение → доставка → возврат в норму.
  **Честный остаток:** ветка «по-прежнему плохо, сигнал уже слали — не повторяем» не
  прогонялась (авария длилась один тик), и не проверялось поведение при мёртвом Postgres —
  тогда сторож обязан взять `chat_id` из кэша. Первое проверяется удержанием юнита в
  простое два тика, второе — только на учебной копии, не на проде.
  `added: 2026-09-04` `last-touch: 2026-09-04`

- **R2/R3 — гигиена из Q3-самоосмотра.** R2: `bearerToken` скопирован в **6** роутов,
  `clientIp` — в **9** → вынести в `lib/api/request.ts`, ~0.3 сессии. Числа 4 и 5 стояли
  здесь с 08-10 и устарели в тот же день: копии растут с каждым новым роутом (`claim` 08-10,
  ЕСА `oidc/{start,callback}` 08-25, `grants` 03-09), а пункт правили последним. Поэтому
  счёт заменён **признаком проверки**, а не новым числом:
  `grep -rc 'function bearerToken\|function clientIp' app/api --include=route.ts | grep -v ':0'`.
  R3: `secrets.ts` **1300** строк и пять зон ответственности → резать по зонам,
  ~0.5 сессии, **строго после R1** (иначе рефакторинг вслепую).
  `added: 2026-08-10` `snoozed: 0` `last-touch: 2026-09-03`

- **R6/R7 — не чинить, ждать условия.** Rate-limit in-memory корректен, пока инстанс один
  (`until:` появился второй инстанс). Аудит секретов растёт без ретенции
  (`until:` >100k строк — тогда он начнёт утяжелять vault-дамп).
  **Прогон 2026-09-04:** инстанс по-прежнему один (в cgroup юнита ровно один PID); аудит —
  **779 строк**, а не 124, как стояло в этом пункте с 08-10: за 25 дней +655, около 26 в
  сутки, основной генератор `grant_read` (304). Порог не близко (годы), но число в
  формулировке успело устареть в шесть раз — ровно так парк и протухает молча.
  `parked` `added: 2026-08-10` `snoozed: 0` `last-touch: 2026-09-04`
- **G54 «поле прошло все слои» (zod-strip drift)** — не задача, а правило, срабатывающее по
  событию: при расширении контракта через optional-поле прогнать живой round-trip (тип → клиент →
  route-валидатор → сервис → хранилище → обратное чтение). strict/strip zod-схема молча срезает
  новые optional-поля, юнит-зелёный это НЕ ловит. Детали — `../brain_matrica/cross-project-ideas/
  GOTCHAS.md` → G54. **Прогон 2026-09-04** (ритм был просрочен: контракт расширился в #125 —
  миграция 0015, `circle_shared_at`, свои поля, перенос реквизитов карточки — а последний
  прогон стоял с 06-13). Три линзы, дрейфа не нашлось: свои поля не идут неизвестными
  ключами через strip-схему, у них отдельное действие с массивом `{name, value}` первого
  класса; гейт круга — `ownership()` в `WHERE` самого `UPDATE`, а чтение идёт единым
  предикатом `documentVisibleTo`, применённым и к списку, и к прямому доступу к файлу по id;
  восемь колонок карточки действительно стали рудиментом (`ProfileFieldKey` сужен до шести
  ключей, `profileFromRow` проецирует явно). Кандидат «ветка правки уходит в `return` до
  сохранения полей» проверен и **опровергнут**: редактор полей обёрнут в `{!isEdit && …}`,
  `defaults()` всегда даёт `fields: []`. Остаток честный, но не дефект: создание документа и
  укладка полей — две операции без транзакции, провал второй виден тостом (тот же класс, что
  наводка brain #281). `recurring` `every: при расширении контракта` `last-run: 2026-09-04`

## Фичи (ждут запроса — parked)

- **#035 Поиск: остаток Ф2+Ф3** — Ф0 (`lib/search/tiered-search.ts`) и Ф1 (подключение в таблицы)
  сделаны в PR #16 (06-10). Остались Ф2 — комбобоксы форм (когда списки селектов вырастут) и
  Ф3 — серверный поиск/пагинация (`ILIKE` + `pg_trgm` GIN, debounce, probe латентности #020).
  План — `mailbox/to-brain/2026-06-10-tiered-search-plan.md`.
  `parked` `until:` БД выросла (~>100 МБ) или владелец пожаловался на скорость списков —
  прогон 2026-09-04: **13 МБ**. Динамика 11 (07-28) → 12 (08-10) → 13 (09-04), около 1 МБ в
  месяц: при таком темпе порог — вопрос лет, а не месяцев.
  `added: 2026-06-09` `snoozed: 1` `last-touch: 2026-09-04`
- **Напоминания P5 — богатый контент** — ядро (P0–P4a) в проде. Осталось: вложения (картинки/сканы
  через `MEDIA_ROOT` + `lib/storage/media`), опросы, несколько получателей, журнал доставок UI,
  библиотека шаблонов. Runbook — `docs/telegram-reminders.md`.
  `parked` `until:` владелец попросил вложения/опросы/несколько получателей.
  `added: 2026-06-14` `snoozed: 1` `last-touch: 2026-07-28`
- **Настройки дайджеста per-user** — время (10:00 МСК) и окна (к оплате 7 дн, документы 30 дн) —
  константы в `lib/reminders/domain-templates.ts`; сделать настраиваемыми в `/settings`.
  `parked` `until:` владелец попросил менять время/окна дайджеста.
  `added: 2026-06-14` `snoozed: 1` `last-touch: 2026-07-28`
- ~~**Множественные доп. файлы документа**~~ — **сделано 2026-09-04 (#118)**, миграция 0011.
  Пункт ждал с 05.06 и пережил два снуза; владелец попросил 03.09, и условие `until:`
  выполнилось. Вместо `additional_files varchar(100)` — таблица `document_file`, до 20 файлов
  на документ, каждый со своим исходным именем и размером. Заодно ушли слоты
  front/back/additional: второй файл в занятый слот больше нечему затирать. Закрыто.
- ~~**Превью-миниатюры сканов** в таблице документов вместо иконок-ссылок~~ — **сделано
  2026-06-28** (`4b95236`, #44), за месяц до последнего ре-триажа этого пункта.
  `ScanThumb` в `components/app/documents-table.tsx` рисует для image-сканов настоящую
  миниатюру (`<img class="h-10 w-10 … object-cover">`) во всех трёх слотах — front, back,
  additional; иконка осталась только fallback'ом для не-картинок. Пункт при этом дважды
  снузили (`snoozed: 2`) и продолжали ждать запроса владельца на то, что уже стояло в проде.
  Найдено аудитом 2026-09-03. Закрыто.

## Тех-долг / инфра

- ~~**Справочник банков общий на всех — чужой банк может удалить любой залогиненный**~~ —
  закрыто 2026-09-04 (#117) РЕШЕНИЕМ, а не миграцией. Владелец: справочник остаётся общим
  («добавлять могут все, чтобы подтянуть к себе банк, где брал кредит; удалять не могут»),
  поэтому `user_id` у `credits_bank` не появился — вместо этого правка и удаление закрыты
  для не-владельца, а счётчик кредитов в списке стал считать СВОИ (был общий и показывал
  приглашённому, сколько займов у остальных в каждом банке). Остаток класса: родственник,
  добавивший банк с опечаткой, исправить её сам не может — ждёт запроса, не долг.
  Признак проверки, что счётчик не разъехался обратно: у пользователя без кредитов колонка
  «Кредитов» на `/banks` обязана быть нулевой во ВСЕХ строках, а список — непустым.
  `added: 2026-09-03` `last-touch: 2026-09-04`
- **Slug комнаты секретов — глобальное пространство имён у неглобального раздела.**
  Раздел `/secrets` по требованию владельца остаётся доступен всем (пустой у каждого), но
  `secrets_project.slug` уникален на всю базу: приглашённый человек, заводя комнату
  «setka», получит отказ и тем самым узнает, что такая комната существует, а при удачном
  имени — займёт имя из пространства экосистемы. Не дыра в данных (чужие комнаты не видны,
  ключи не читаются), а утечка существования и возможность сквота.
  `added: 2026-09-03` `snoozed: 0` `last-touch: 2026-09-03`

- ~~**Бэкап `media/` на проде — «процедуры бэкапа нет»**~~ — формулировка была **неверна с
  11 июля**, и три переноса подряд её не поймали, потому что пункт ни разу не сверяли с
  боксом. Проверено 2026-08-10 на проде: `media/` входит в Ф4-бэкап (`backup_vault.sh`),
  cron 03:30 работает, на Диске 14 gpg-архивов, шифрование — на подключ владельца,
  закрытого ключа на боксе нет (как и задумано). Сканов при этом ноль: в
  `documents_document` **0 строк**, то есть «риск потери данных» был нулевой не из-за
  качества бэкапа, а потому что терять нечего. Закрыто; остаток вынесен отдельным пунктом
  ниже — он про восстановление, а не про бэкап.

- **Учебное восстановление бэкапа ни разу не прогоняли** — и в одиночку прогнать нельзя:
  расшифровка требует закрытого gpg-ключа владельца (на боксе его нет — правильно).
  Проверено, что архивы зашифрованы на верный подключ и долетают целиком (с 08-10 скрипт
  сверяет размер на Диске через HEAD и пишет число упакованных файлов), но **что архив
  расшифровывается — не проверял никто**. Drill — `docs/secrets-vault-backup.md`, раздел
  «Учебное восстановление»: скачать → `gpg --decrypt` → сверить с `MANIFEST.txt` → поднять
  `vault.sql` в локальную базу и прочитать один секрет мастер-ключом. Отдельный вопрос
  владельцу: **где лежит единственная копия закрытого ключа** — если на одной машине, то
  её смерть делает нечитаемыми все архивы разом.
  **Прогнано владельцем 2026-08-15** с сопровождением brain, целиком: скачивание с Диска →
  расшифровка закрытым ключом (то самое непроверенное звено) → распаковка → `vault.sql`
  71 770 Б, 9 таблиц с данными → `media.tar.gz` пуст, что сходится с «документов 0».
  Побочный урок в нашу пользу: закрытого ключа на дежурной машине владельца **не было**,
  нашёлся экспортом в домашней папке — «ключ есть» оказалось утверждением, требующим прогона.
  Расхождение манифеста, найденное тем же прогоном, объяснилось не докой и не скриптом,
  а третьей копией: прод исполнял `backup_vault.sh` от 11 июля (см. пункт про scripts/ ниже).
  `recurring` `every: квартал` `last-run: 2026-08-15` (следующий — Q4)
- **Migration-guard защищает один push, а не инвариант** — шаг `Migration guard` в
  `deploy-prod.yml` смотрит `git diff HEAD~1 HEAD` по `lib/db/migrations/*.sql`, то есть отвечает
  на «принёс ли ЭТОТ коммит миграцию», хотя защищать должен «код не уезжает раньше схемы».
  Следствие: сразу после мержа миграционного PR любой следующий code-коммит проходит guard и
  катит прод на непримененную схему. 2026-07-28 не выстрелило случайно — следующий PR трогал
  только `docs/`/`mailbox/`/`.claude/` (они в `paths-ignore`). Лечение: журнал `schema_migrations`
  в БД + шаг деплоя «все файлы миграций присутствуют в журнале, иначе стоп» — тогда защита
  снимается сама и `workflow_dispatch` перестаёт быть ручным обходом. Грабля отправлена мозгу
  (`mailbox/to-brain/2026-07-28-migration-guard-window-gotcha.md`).
  **Ре-триаж 2026-09-01:** формулировка сверена с кодом — `deploy-prod.yml` по-прежнему
  смотрит `git diff HEAD~1 HEAD`, дефект живой, лечение не устарело. Не берём в работу:
  очередь Q4 занимает R1 (сервисный слой без тестов), а он блокирует R3. Снуз, не парковка —
  проверяемого условия расконсервации у пункта нет, он просто ждёт своей очереди.
  `added: 2026-07-28` `snoozed: 1` `last-touch: 2026-09-01`
- **Напоминания P3 — остаток движка расписаний:** праздники (к режиму «только рабочие дни») и cron.
  В проде: день/неделя/месяц/год, интервал, окончание, тихие часы, только-рабочие-дни, календарь
  будущих срабатываний в форме (`nextFires`), произвольные даты (`kind:'dates'`, #47).
  **Ре-триаж 2026-09-01: парковка вместо третьего снуза.** За два с половиной месяца спроса
  ни разу не было: режим «только рабочие дни» в проде и владельца устраивает, про праздники
  и cron не спрашивал никто. Пункт, который дважды снузили без запроса, — это фича по
  требованию, а не долг. `parked` `until:` владелец попросил учёт праздников или cron-выражения.
  `added: 2026-06-14` `snoozed: 1` `last-touch: 2026-09-01`
- **Волна 2 ADR-0012 — паспортный вход в vault.** **Веха 1 сделана 2026-08-10** (`18c9326`,
  PR #94): роуты `POST /api/secrets/session` / `GET /api/secrets/self` / `DELETE …/session`,
  миграция `0007` на проде, верификатор с полным набором проверок §5 (4 позитива /
  13 негативов), JWKS с cooldown+stale-if-error, каскад отзыва + проверка отзыва личности
  на каждом чтении, долг §6 закрыт целиком, break-glass за `PROVISION_KEY_ENABLED`.
  Контракт и runbook — `docs/passport-server.md`, отчёт brain отправлен.
  **Веха 2 сделана 2026-09-03** (`22bd6e5`, PR #113): раздел `/secrets/identities` —
  заведение формой под superuser, отзыв каскадом с числом погашенных сессий, аудит
  `identity_created`/`identity_revoked`. Реестр руками в psql больше не ведётся, SQL-runbook
  в `docs/passport-server.md` остаётся аварийным путём. **Обе вехи закрыты; волна 2 целиком
  выполнена**, пункт держится только как оглавление — что и где сделано.
  `added: 2026-08-03` `snoozed: 0` `last-touch: 2026-09-03`

- **Список таблиц в бэкапе не связан со схемой** — `scripts/backup_vault.sh` перечисляет
  таблицы дампа явно (и правильно: иначе в vault-бэкап уедут кредиты и документы), но ничто
  не связывает этот список с миграциями. Следствие поймали 08-10: `secrets_grant` не попадал
  в бэкап **с миграции 0006**, то есть grant-примитив месяц жил без бэкапа — при откате БД
  карта выдач восстанавливалась бы руками. Разово починено (добавлены `secrets_grant` и
  `passport_*`), но класс остался: следующая миграция с новой таблицей повторит промах.
  Лечение — гейт «каждая таблица схемы либо в дампе, либо в явном списке исключений»
  (исключения сегодня: `passport_jwks_cache` — кеш, восстановим фетчем). Отправлено brain
  письмом 08-10 как кандидат в общий гейт экосистемы.
  **Третий рецидив 2026-09-04.** Пять новых таблиц эпика (`person_profile`, `document_field`,
  `document_file`, `circle`, `circle_member`) в дамп не попали, и вместе с ними — давно
  отсутствовавшие `documents_document`, `documents_documentcategory`, `auth_oidc_identity`.
  Сочетание было худшим из возможных: `media.tar.gz` увозил СКАНЫ документов, а строки,
  которые говорят, чей это скан и что в документе написано, не увозил никто —
  восстановление дало бы папку безымянных картинок. Разово починено тем же PR (#123);
  класс по-прежнему жив, и это уже третий раз, когда его ловит человек, а не гейт.
  Признак проверки: `pg_dump --schema-only -t <каждая таблица из lib/db/schema.ts>` против
  списка в скрипте — расхождение обязано быть пустым либо объяснённым в `excludes:`.
  `added: 2026-08-10` `snoozed: 0` `last-touch: 2026-09-04`
- **Переоткрытие комнаты владельцем (кнопка в GUI) + амендмент ADR-0010** — гейт
  `provisionFirstToken` требует «0 секретов, 0 карточек, токен не использован», то есть считает
  признаком живой комнаты её **наполненность**. Наполняет комнату владелец, и делает это до
  онбординга — значит подготовка к заселению сама запирает дверь. Наступили на это 2026-08-10
  с комнатой `gonba`: секрет положили в GUI в 09:41, self-serve перестал работать, токен
  выдавали транзакцией на проде. Ослаблять условие нельзя (проверка на пустоту защищает
  содержимое заранее наполненных комнат от держателя `VAULT_PROVISION_KEY`) — лечение в том,
  чтобы дать владельцу явный ход, который текст отказа уже обещает: «переоткрытие только
  владельцем» под 2FA, а кнопки нет. Отправлено brain письмом 08-10.
  **Ре-триаж 2026-09-04 (всплыл по возрасту):** запарковать. После 08-10 self-serve
  provisioning не понадобился ни разу — все новые комнаты заходят паспортом (D-061), а
  личность заводится из GUI реестра. Кнопка нужна только при повторении случая `gonba`.
  `parked` `until:` владелец заранее наполнил комнату и провалил `provisionFirstToken`
  (строка `provision_denied` в аудите с текстом про непустую комнату).
  `added: 2026-08-10` `snoozed: 0` `last-touch: 2026-09-04`
- **Менеджер секретов (#48) — в проде, развитие по запросу.** MVP + запись по токену (#54),
  комнаты всей экосистемы (#58), vault-расширение целиком (Ф1 карточки #61, Ф2 TOTP 2FA+hardening
  #62, Ф3 CSV-импорт #64, Ф4 gpg-бэкап vault+media/ на Яндекс.Диск с ночным cron 03:30 #65),
  self-serve provisioning под `VAULT_PROVISION_KEY` (#68), **выдача доступа между комнатами
  (grant, PR #73 — мандат brain 07-26, идея #091)**. Детали — `docs/secrets-manager.md`,
  контракт — `docs/secrets-client-guide.md`.
  Остаток: ротация мастер-ключа, scoped-по-ключам/TTL-токены, экспорт/импорт `.env`, CLI-клиент.
  `parked` `until:` brain/владелец попросил конкретный пункт остатка.
  `added: 2026-06-28` `snoozed: 0` `last-touch: 2026-07-28`

- **Сверка `scripts/` репозитория с боксом — признак проверки для класса «двойник вне git».**
  2026-08-25 нашли, что прод исполнял `backup_vault.sh` от 11 июля: деплой возил только
  `.next/standalone`, а `scripts/` на боксе был ручной копией. Следствие — реестр паспортных
  личностей, карта grant'ов и времянки не попадали в бэкап полтора месяца. Причина устранена
  (деплой возит `scripts/`), но класс шире: у файла в репозитории может быть исполняемый
  двойник, которого не касается ни один гейт. Признак проверки:
  `git ls-files scripts/ | while read f; do ssh karman "cat <база>/$f" | diff -q - "$f" >/dev/null || echo "РАСХОДИТСЯ: $f"; done`
  `added: 2026-08-25` `snoozed: 0` `last-touch: 2026-08-25`
- ~~**Вход через ЕСА — задача владельца от 2026-08-25**~~ — **сделано и на проде** тем же днём.
  Клиент зарегистрирован самообслуживанием у setka (ADR-0010), контур целиком:
  authorization_code + PKCE (S256), RS256/JWKS, nonce, одноразовое состояние в подписанной
  cookie; TOTP спрашивается и после ЕСА. Проверено с бокса: провайдер клиента принял (увёл
  на свою форму, сохранив запрос). Детали и решения — `docs/esa-login.md`, письмо brain —
  `mailbox/to-brain/2026-08-25-karman-is-now-an-esa-client.md`. Закрыто 2026-08-25.

- **Круг ЕСА браузером владельца — чужой ход.** Пройти может только он: кнопка «Войти через
  ЕСА вМалмыже» → форма ЕСА (в т.ч. ВКонтакте) → возврат → код из Google Authenticator →
  главная. Признак закрытия — в `auth_audit` пара строк `esa_ok_totp_pending:linked_by_email`
  и следом `login_ok_totp`. Если вместо привязки заведётся ВТОРОЙ пользователь — значит
  почта в ЕСА не совпала с `valstan@valstan.ru` или пришла без `email_verified`; лечится
  либо правкой почты в профиле ЕСА, либо привязкой руками.
  `until:` владелец прошёл круг. `added: 2026-08-25` `last-touch: 2026-08-25`

- ~~**Веха 2 паспорта — GUI реестра личностей в `/secrets`**~~ — сделано 2026-09-03:
  раздел `/secrets/identities`, заведение формой (только superuser), отзыв каскадом с
  показом числа погашенных сессий, аудит `identity_created`/`identity_revoked`, колонка
  «Последний вход» как признак опечатки в номере репозитория. Закрыто.
- **Реестр личностей ЕСА (людей) — отдельная веха, не «одна строка».** Разбор 2026-09-03
  нашёл четыре причины не делать её попутно: отвязка — физический `DELETE` (у
  `auth_oidc_identity` нет `revoked_at`); таблицы нет в списке `scripts/backup_vault.sh`,
  то есть отменить нечем; у `auth_audit` нет колонки `detail`, и какую связь убрали —
  не восстановить; гейт «не запереть человека снаружи» не пишется очевидным предикатом,
  потому что пользователю, заведённому через ЕСА, кладётся структурно валидный хэш
  заведомо непригодного пароля (`lib/services/oidc-login.ts`), и проверка «пароля нет»
  на нём всегда ложна. Признак проверки перед началом:
  `select origin, count(*) from auth_oidc_identity group by origin`.
  `parked` `until:` владелец попросил отвязку личности ЕСА из интерфейса, или появился
  второй человек с входом через ЕСА. `added: 2026-08-25` `snoozed: 0` `last-touch: 2026-09-03`

## Выкинуто (с причиной — чтобы не всплывало заново)

- **Дашборд AI-расходов (ccusage)** — suggest-идея brain 06-04 (ccusage → Postgres → recharts +
  TG-алёрты). Три ре-триажа (07-10, 07-28) интереса владельца не выявили; прошлый ре-триаж сам
  пометил её кандидатом на выкидывание. Выкинута 2026-07-28 — вернётся из git, если появится
  запрос. Попутный вопрос «жив ли Cursor Model Monitor старого стека» снят: старый стек с боксов
  ушёл вместе с катовером.
- ~~**`GATEWAY_KEY_VMALMYZHE` в комнате `vmalmyzhe`**~~ — круг замкнулся, закрыто 2026-08-10.
  С нашей стороны было сделано всё ещё 08-03 (first-token provision #78/ADR-0010, grant
  `vmalmyzhe → setka` id 2, токен `skm_QQjtIuuM` выдан и передан владельцу файлом). Дальше
  ход был портала — и он его сделал: по письму brain 08-07 портал читает свою комнату живым
  токеном, а в аудите vault от 08-09 стоят парные `pull` (комната setka) + `grant_read`
  (`комната «setka» прочитала GATEWAY_KEY_VMALMYZHE по выданному доступу`). Проверка, которую
  мы записывали как критерий (`404` сменился на `200`), выполнена не нами и не в чате, а самим
  трафиком.
- ~~Удалить тестовое напоминание id 1~~ — закрыто 07-17: записи с id 1 в `reminder` уже нет.
- ~~Удалить смоук-комнату `provsmoke` (id 12)~~ — удалена 07-17 (cascade токен+аудит); заодно
  удалён мёртвый авто-аккаунт `telegram_1829863265`. Probe 07-28 подтвердил: комнаты нет.
- ~~Удалить комнату-сироту `rmzmalmyzh` (id 13)~~ — хвост реестра brain (письмо 07-26: rw-токен
  утерян при доставке через лог). Удалена владельцем через UI 2026-07-28 (cascade секрет+токен+
  аудит); живая `rmz` (id 14) не тронута. В реестре 12 комнат.
- ~~Временные комнаты round-trip'а `granttest-src`/`granttest-dst` (id 15, 16)~~ — заведены и
  удалены 2026-07-28 в рамках живой проверки grant'а; внутри были только dummy-значения.

## Заметки (не задачи)

- Калькулятор досрочного погашения — модель аннуитета; для дифференцированных кредитов считает
  приближённо (дисклеймер в карточке есть). Точная модель — только если попросит владелец.
