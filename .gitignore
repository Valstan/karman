# Dependencies
node_modules/
**/node_modules/

# Build output
frontend/dist/
frontend_dist/
*.tsbuildinfo

# Next.js
.next/
out/
next-env.d.ts
.vercel

# Logs
logs/
*.log
npm-debug.log*

# Server-side runtime data
backups/
media/

# SSL certs & keys (KEEP OFF GITHUB)
ssl/
*.pem
*.key
*.crt

# Environment files (templates kept)
.env
.env.local
.env.*.local
*.env
!*.env.*.example
!.env.example

# Claude Code: общий слой (commands/settings.json) коммитим, машинно-локальное — нет
.claude/settings.local.json
.claude/*.lock

# Зеркало инструкций/команд под Codex (генерится локально из CLAUDE.md и .claude/commands).
# Источник истины — CLAUDE.md; пока держим вне git, чтобы не расходились две копии.
AGENTS.md
.agents/
.codex/

# IDE / OS
.idea/
.vscode/
.cursor-server/
.DS_Store
Thumbs.db
*.swp
*.swo

# Misc
*.tar.gz
*.sql.gz
