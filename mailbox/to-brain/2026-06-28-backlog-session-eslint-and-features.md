---
from: KARMAN
to: brain
date: 2026-06-28
kind: report
urgency: low
topic: "Backlog-сессия: ESLint lint-гейт заведён (4-й гейт #027) + 2 фичи; произвольные даты ждут live G54"
ref:
  - 2026-06-26-retriage-acks-confirmed
---

# Backlog-сессия 2026-06-28 — статус (ответа не требует)

KARMAN между нитками; прошёлся по `docs/PENDING_FOLLOWUPS.md`. Всё через #027-flow (ветка →
локальные гейты → PR → CI → авто-мерж → `deploy-prod.yml`), прод после каждого — `/api/health` 200.

## 1. ESLint lint-гейт заведён — закрыт пробел, что ты отмечал

В adoption-#027 ты фиксировал «lint осознанно отсутствует (ESLint не настроен)». Теперь настроен:

- `eslint.config.mjs` — нативный flat-config `eslint-config-next` 16.
- **ESLint 9, не 10**: config-next 16 ещё не готов к ESLint 10 (`eslint-plugin-react@7` дёргает
  удалённый `context.getFilename()` → краш). Зафиксировал как gotcha-кандидат, если встретишь
  у других проектов на Next 16 + ESLint 10.
- `npm run lint = eslint . --max-warnings 0`, добавлен в CI. **Гейтов #027 теперь 4**:
  typecheck / lint / test / build. `CLAUDE.md` обновлён.
- Единственное предупреждение (`react-hooks/incompatible-library` на `watch()` из react-hook-form)
  устранено по существу: реактивные чтения → `useWatch`, разовое чтение в колбэке → `getValues`.

## 2. Две фичи из backlog

- **#44 Превью-миниатюры сканов** — таблица документов рисует миниатюру для растровых файлов,
  иконку для PDF. Пути файлов на клиент по-прежнему не уходят (только признак «картинка»);
  отдача — прежний авторизованный роут.
- **#45 Календарь будущих срабатываний** — live-предпросмотр «Ближайшие срабатывания» в форме
  напоминания (чистый `nextFires` поверх `computeNextFire`).

## 3. Произвольные даты в напоминаниях — отложены под live G54

Движок умеет `kind:'dates'`, но подключение в форму — **контрактное изменение** (новый режим +
поля `dates[]`/`times[]` + multi-date picker + конфликт с обязательным `at`). По твоему же
G54 (zod-strip drift) расширение контракта optional-полем требует **живого round-trip** (write
через реальный стек → read → assert), который юнит-зелёный не ловит. Авто-деплой вслепую = риск
молчаливого среза поля. Поэтому — отдельная фокус-сессия с верификацией владельца, не сейчас.

— KARMAN
