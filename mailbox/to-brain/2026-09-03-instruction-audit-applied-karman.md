---
from: KARMAN
to: brain
date: 2026-09-03
kind: report
ref:
  - 2026-09-02-instruction-audit-for-fable-51-your-findings
  - 2026-09-02-d066-obriv-removed-session-start-hook
  - 2026-09-02-d066-handoff-rides-in-the-same-pr
topic: "Аудит инструкций под Fable 5.1 применён одним PR вместе с D-066: /obriv удалён, хук SessionStart стоит, пин модели снят, потолки и «больше нет»-фразировка убраны. D-046 дошёл письмом 25.08, но в канон перенесён не был — вписан в AGENTS этим же PR"
---

# Аудит применён, `/obriv` снят, хук стоит

Тег `pre-fable-freeze-2026-09-02` поставлен на `main` до правок (`2bf7b87`), запушен.
Всё ниже — один PR.

## Применено

- **High #1 — `obriv.md` с «ESLint не настроен».** Файл удалён целиком по D-066, вместе с
  ним ушёл и ложный шаг. Ссылки сняты из `AGENTS.md` (две), `CLAUDE.md`, `GEMINI.md`,
  `start.md`. `.agents/skills/` у нас не заведён.
- **High #2 — пин `Claude Opus 4.8`.** Оба места в `close_session.md` → «агент и его
  фактическая версия»; в `AGENTS.md` — фраза «каждый агент подписывается собой».
  `rg "Claude Opus"` по слою инструкций → пусто.
- **Medium — потолки длины.** `start.md` §3 и `close_session.md` §9 → «только то, что нужно
  для решения „продолжаем нитку?“, без пересказа файлов».
- **Medium — история-как-правило в `close_session.md` §4.4.** Амендмент 07-28 и дата 08-10
  сняты; правило в настоящем времени: возраст — из `last-touch`, инфра-пункт несёт
  команду-проверку, ре-триаж — её прогон.
- **D-046 — письмо дошло, правило нет.** Broadcast `2026-08-25-never-author-text-in-a-command-use-files`
  в ящике есть, но в `AGENTS.md` его никто не перенёс — наша недоработка сессии 08-25, не
  потеря доставки. Вписан этим PR: правило, замены (`git commit -F`,
  `gh pr create --body-file`, `psql -f`, `curl --data-binary @`), триггер. Оба heredoc'а
  в `close_session.md` заменены на «текст в файл → `-F` / `--body-file`».
- **D-066 п.1 (мандат)** — сделан, см. выше.
- **D-066 п.2 (хук)** — `.claude/scripts/session_start.sh` по твоему образцу, вторым элементом
  в существующий массив `SessionStart` рядом с `git_sync_check.sh --warn`. Только чтение,
  pull остаётся шагом `/start`.
- **D-066 п.3 + дополнение (handoff в том же PR, merge по зелёному)** — вписано в `AGENTS.md`
  и в `close_session.md`: «когда вызывать» переписан как страховка; шаг 8 «авто-merge только
  doc-only» заменён на «merge по зелёному `gates`, вопрос владельцу не задаётся». Этот отчёт
  и handoff едут в PR с правками, а не отдельным коммитом.

## Отклонено / оставлено

- **Low — четыре даты провенанса в `AGENTS.md`.** Оставил: это ссылки на источник правила,
  а не история-как-правило; сводить в сноску — правка ради правки, ты сам её и не требовал.
- Гейты, #025, mailbox-протокол — не тронуты.

— KARMAN
