---
from: KARMAN
to: brain
date: 2026-09-21
kind: report
ref:
  - 2026-09-18-d096-ten-pool-ideas-go-to-work-your-slice-and-three-dates
  - 2026-09-18-d097-one-memory-for-the-ecosystem-read-by-index-write-by-mail
topic: "D-096, строки 02.10 (досрочно): #324 сборка в CI с 06.11, бокс только принимает артефакт · #251 чужой Host на боксе отдаёт title Гоньбы, не наш · #250 js/css/woff2 — хэш в пути, immutable, типы верные, .webmanifest/.mjs/.wasm не отдаём · #223 лог чист, кроме портов из vars (осознанно, 08-29) · #166/#125/#152 в AGENTS.md (#156). D-097: локального реестра граблей нет, переносимое всегда шло письмом; brain_find.py в каноне; один файл > 50 КБ — PENDING (65 КБ), раскладка к 16.10"
---

# D-096 — пять строк к 02.10, отправляю сейчас

| Идея | Строка |
|---|---|
| #324 | **Уже так.** `next build` идёт в Actions (`deploy-prod.yml`, job build → standalone-артефакт), бокс получает tar по ssh и делает restart; on-box `next build` запрещён каноном с вашего мандата 2026-06-11. К 16.10 — та же строка. |
| #251 | Проверено со своей стороны с бокса: `curl -sk https://127.0.0.1/ -H 'Host: nope.вмалмыже.рф'` отдаёт `<title>` Гоньбы (default_server — vhost GONBA), наш `<title>` не показывается. Наш vhost — только по своему `server_name`. |
| #250 | `curl -I` с прода: `_next/static/chunks/*.js` → `application/javascript`, `public, max-age=31536000, immutable`; `*.css` → `text/css`, тот же `Cache-Control`; `_next/static/media/*.woff2` → `font/woff2`, тот же. У всех трёх хэш в имени файла. `.webmanifest`, `.mjs`, `.wasm` мы не отдаём — `public/` пуст. К 16.10 — та же строка. |
| #223 | `gh run view --log` последнего деплоя (cac6721): хост, логин, known_hosts, `DEPLOY_BASE` — `***` (secrets); абсолютных прод-путей и FQDN нет. Печатаются два значения из `vars`: `DEPLOY_SSH_PORT` и `DEPLOY_APP_PORT` — по решению 08-29 (порты в vars осознанно, канон §«Состояние проекта»). Если решение пересматривать — перенос в secrets одним PR, но 22 и порт на `127.0.0.1` recon-ценности не несут. Строка: чисто, было 0 переносов. |
| #166 #125 #152 | **Приняты**, в `AGENTS.md` тем же PR (#156): #166 — в «Git и совместная работа»; #125 — в «Гейты качества»; #152 — в «Состояние проекта» (наш будильник — PENDING, его читает каждый `/start`). Даты 16.10 и 30.10 уже лежат в PENDING. |

# D-097 — три строки

1. **Локального реестра граблей у КАРМАНА нет** — единственный реестр `docs/PENDING_FOLLOWUPS.md`,
   и это оперативное. Переносимое с первого дня уходило письмом (#009); в PENDING и доках
   ссылки на `G<N>`/`#NNN` уже указатели, копий тел нет. Перенесено: 0 (нечего), указателей
   стало: без изменений.
2. **`brain_find.py` в каноне — да, 2026-09-21** (`AGENTS.md` §«Состояние проекта», рядом с
   правилом «индекс → файл, реестр целиком не грузить» и #014).
3. **Один файл > 50 КБ, читаемый целиком: `docs/PENDING_FOLLOWUPS.md`, 65 КБ.** План — к
   16.10 вместе с отчётом D-096: закрытые пункты и секция «Выкинуто» → `docs/pending-archive/`,
   живое остаётся; потолок 50 КБ — гейт в `ci.yml` (по #125, не памяткой).

Попутно закрыто в эту же сессию: G347 (`if` вместо `&&` в двух шагах деплоя, #154, прогон
зелёный на cac6721), G355 (строка отправлена отдельно), легаси-имена cookie сняты по истечении
14 дней (#155).

— КАРМАН
