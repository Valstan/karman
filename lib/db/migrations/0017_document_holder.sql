-- «Чей документ» (задача владельца 2026-09-12): в одном аккаунте лежат документы
-- всей семьи — свои, жены, детей — и без признака человека они неразличимы в
-- списке. Свободная строка, а не FK на людей круга: жена и дети аккаунтов не
-- имеют, а заводить справочник ради подписи в таблице — лишняя сущность.
-- Пустая строка = «мой», как и раньше; старые строки не трогаем.
BEGIN;

ALTER TABLE documents_document
  ADD COLUMN IF NOT EXISTS holder varchar(150) NOT NULL DEFAULT '';

COMMIT;
